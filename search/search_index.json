{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"loft-cli","text":"<p>A self-hosted infrastructure compiler \u2014 turns a typed YAML spec into a reviewable plan, deterministic execution, and human-readable ops docs for fresh Linux servers.</p> <p>loft-cli is Layer 1. It makes VMs ready and usable: bootstrap, harden, install services, deploy containers, manage configuration, detect drift, and verify state \u2014 all from a single typed YAML spec.</p>"},{"location":"#why-loft-cli","title":"Why loft-cli?","text":"<p>When you provision a server manually, the steps live in your head. With Ansible playbooks or shell scripts, the intent is buried in imperative code. loft-cli takes a different approach:</p> <ol> <li>You write what you want (a YAML spec)</li> <li>loft-cli generates what it will do (a reviewable plan)</li> <li>You approve, then it executes exactly that plan</li> </ol> <p>What you review is exactly what executes. Every time.</p>"},{"location":"#quick-install","title":"Quick Install","text":"pipBinary (Linux)Binary (macOS)Docker <pre><code>pip install loft-cli\n</code></pre> <pre><code>curl -L https://github.com/1ops-eu/loft-cli/releases/latest/download/loft-cli-linux-amd64 \\\n  -o /usr/local/bin/loft-cli\nchmod +x /usr/local/bin/loft-cli\n</code></pre> <pre><code>curl -L https://github.com/1ops-eu/loft-cli/releases/latest/download/loft-cli-macos-arm64 \\\n  -o /usr/local/bin/loft-cli\nchmod +x /usr/local/bin/loft-cli\n</code></pre> <pre><code>docker run --rm ghcr.io/1ops-eu/loft-cli:latest --help\n</code></pre>"},{"location":"#the-four-step-workflow","title":"The Four-Step Workflow","text":"<pre><code># 1. Validate \u2014 check your spec for errors\nloft-cli validate my-server.yaml\n\n# 2. Plan \u2014 see exactly what will happen\nloft-cli plan my-server.yaml\n\n# 3. Docs \u2014 generate a Markdown runbook\nloft-cli docs my-server.yaml -o RUNBOOK.md\n\n# 4. Apply \u2014 execute the plan\nloft-cli apply my-server.yaml\n</code></pre>"},{"location":"#what-you-get-from-one-spec","title":"What You Get From One Spec","text":"<p>From a single YAML spec, loft-cli produces:</p> <ul> <li>A secure, hardened Linux server (SSH key-only, custom port, ufw firewall, optional WireGuard VPN)</li> <li>A Markdown runbook you can put in your wiki</li> <li>A local <code>~/.ssh/conf.d/</code> entry so <code>ssh prod-1</code> just works</li> <li>A local inventory with full historization</li> </ul>"},{"location":"#spec-kinds","title":"Spec Kinds","text":"Kind What it does <code>bootstrap</code> Harden a fresh Debian/Ubuntu server <code>service</code> Install PostgreSQL, Nginx, Docker, containers <code>file_template</code> Render managed config files from Jinja2 templates <code>compose_project</code> Deploy Docker Compose stacks <code>stack</code> Group resources with dependency ordering <code>http_check</code> HTTP readiness probe with retry/backoff <code>systemd_unit</code> Host-native systemd services <code>systemd_timer</code> Scheduled execution via systemd <code>backup_job</code> Backup with retention and scheduling <code>postgres_ensure</code> Ensure PostgreSQL users, databases, extensions"},{"location":"#what-loft-cli-is-not","title":"What loft-cli is NOT","text":"<ul> <li>Not a general-purpose config management system (not Ansible)</li> <li>Not a Kubernetes orchestrator</li> <li>Not a UI/SaaS product</li> <li>Not an application-level orchestrator \u2014 it does not configure SaaS apps, import workflows, or seed business data</li> </ul> <p>loft-cli is Layer 1. Once it's done, the server is a functioning platform. Application-level configuration belongs in a separate orchestration layer.</p>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Getting Started \u2014 install, write your first spec, bootstrap a server</li> <li>Concepts \u2014 understand the pipeline, agent architecture, and safety model</li> <li>Spec Reference \u2014 full field-by-field documentation for every kind</li> </ul>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to loft-cli are documented in this file.</p> <p>The format follows Keep a Changelog. loft-cli uses Semantic Versioning.</p>"},{"location":"changelog/#unreleased","title":"[Unreleased]","text":""},{"location":"changelog/#100-2026-04-10","title":"[1.0.0] - 2026-04-10","text":""},{"location":"changelog/#features","title":"Features","text":"<ul> <li>docs: MkDocs + Material docs site with full spec reference, CLI reference, concepts, and operations guides</li> <li>docs: GitHub Pages publishing workflow on every push to main and version tags</li> <li>release: GPG-signed release artifacts \u2014 <code>checksums.txt.asc</code> published alongside binaries</li> <li>release: git-cliff changelog auto-generation on every release tag</li> <li>ci: Coverage reporting with <code>pytest-cov</code> (60% threshold enforced in CI)</li> <li>ci: <code>make test-cov</code> target for local coverage reports</li> <li>agent: Version compatibility enforcement \u2014 client checks agent major version at connect time and exits with a clear error if mismatched</li> </ul>"},{"location":"changelog/#documentation","title":"Documentation","text":"<ul> <li>CONTRIBUTING.md: Contributor guide with PR process, commit convention, new-kind checklist, and backwards-compatibility policy</li> <li>CHANGELOG.md: Retroactive changelog from v0.1.0 through v1.0.0</li> <li>loft-cli-releases.pub.asc: GPG public key committed to repo for release verification</li> </ul>"},{"location":"changelog/#0110-2026-03-15","title":"[0.11.0] - 2026-03-15","text":""},{"location":"changelog/#features_1","title":"Features","text":"<ul> <li>multi-host: Sequential apply across selected hosts (<code>fleet_apply</code>)</li> <li>multi-host: Aggregated drift detection across host groups (<code>fleet_doctor</code>)</li> <li>multi-host: Inventory selectors (<code>role=worker</code>, <code>env=staging</code>, etc.)</li> <li>ci: Restored green build across all workflows after v0.10 integration</li> </ul>"},{"location":"changelog/#0100-2025-12-01","title":"[0.10.0] - 2025-12-01","text":""},{"location":"changelog/#features_2","title":"Features","text":"<ul> <li>multi-host: Multi-host inventory with attribute selectors</li> <li>multi-host: <code>kind: fleet_apply</code> \u2014 sequential apply across selected hosts</li> <li>multi-host: <code>kind: fleet_doctor</code> \u2014 aggregated drift validation across a fleet</li> </ul>"},{"location":"changelog/#090-2025-09-15","title":"[0.9.0] - 2025-09-15","text":""},{"location":"changelog/#features_3","title":"Features","text":"<ul> <li>blueprints: <code>kind: blueprint</code> \u2014 reusable composition primitives</li> <li>blueprints: Include semantics (local and repo-relative)</li> <li>blueprints: Parameterization with defaults</li> </ul>"},{"location":"changelog/#080-2025-07-01","title":"[0.8.0] - 2025-07-01","text":""},{"location":"changelog/#features_4","title":"Features","text":"<ul> <li>catalog: Catalog registry (8th open registry) with <code>CatalogEntry</code>, <code>StepTemplate</code>, <code>OutputTemplate</code> models</li> <li>catalog: <code>loft-cli catalog list|show|export</code> CLI commands</li> <li>catalog: Condition DSL for step templates (field presence, equality, boolean)</li> <li>catalog: Service output declarations (connection params, paths, URLs) per kind</li> <li>catalog: Addon-registrable catalog entries</li> <li>schema: Pydantic <code>Field(description=...)</code> enrichment on all ~70 spec schema fields</li> </ul>"},{"location":"changelog/#070-2025-05-01","title":"[0.7.0] - 2025-05-01","text":""},{"location":"changelog/#features_5","title":"Features","text":"<ul> <li>compose: <code>post_deploy</code> steps on <code>compose_project</code>: <code>shell</code>, <code>container_exec</code>, <code>http_request</code></li> <li>compose: <code>project.files</code> \u2014 plain file uploads with per-entry <code>mode</code> and <code>owner</code></li> <li>compose: Compose rebuild detection / <code>rebuild: true</code></li> <li>bootstrap: <code>firewall.allow_ports</code> \u2014 declarative additional port openings</li> <li>package: <code>kind: package</code> \u2014 declarative apt/apk package installation</li> <li>cli: <code>loft-cli logs &lt;host&gt; &lt;service&gt;</code> \u2014 stream container logs without SSH</li> </ul>"},{"location":"changelog/#064-2025-02-15","title":"[0.6.4] - 2025-02-15","text":""},{"location":"changelog/#bug-fixes","title":"Bug Fixes","text":"<ul> <li>agent: Restore idempotency on <code>ensure_goss_dir</code> for test 14</li> <li>agent: Fix chown in <code>ensure_goss_dir</code> to preserve snapshot integrity</li> </ul>"},{"location":"changelog/#063-2025-01-20","title":"[0.6.3] - 2025-01-20","text":""},{"location":"changelog/#features_6","title":"Features","text":"<ul> <li>wireguard: SSH-over-tunnel safety gate \u2014 verifies SSH through VPN before locking SSH to tunnel-only</li> <li>wireguard: <code>loft-cli tunnel up|down|status</code> \u2014 first-class WireGuard tunnel management</li> <li>wireguard: Per-host client-side interface naming (<code>wg-{host}</code>) \u2014 multiple tunnels coexist</li> <li>wireguard: SSH config uses VPN IP when WireGuard is active</li> <li>cli: <code>loft-cli remove &lt;host&gt;</code> \u2014 clean lifecycle endpoint for decommissioned machines</li> </ul>"},{"location":"changelog/#062-2024-12-01","title":"[0.6.2] - 2024-12-01","text":""},{"location":"changelog/#bug-fixes_1","title":"Bug Fixes","text":"<ul> <li>bootstrap: Fix SSH socket detection on Ubuntu 24.04+ (socket-activated sshd)</li> <li>goss: Cross-distro SSH enabled check via command fallback chain</li> </ul>"},{"location":"changelog/#061-2024-11-15","title":"[0.6.1] - 2024-11-15","text":""},{"location":"changelog/#bug-fixes_2","title":"Bug Fixes","text":"<ul> <li>postgres: Fix <code>postgres_ensure</code> idempotency on re-apply when extensions already exist</li> <li>agent: Improve mutation lock release on apply failure</li> </ul>"},{"location":"changelog/#060-2024-11-01","title":"[0.6.0] - 2024-11-01","text":""},{"location":"changelog/#features_7","title":"Features","text":"<ul> <li>kinds: <code>kind: systemd_unit</code> \u2014 host-native systemd services with optional logrotate</li> <li>kinds: <code>kind: systemd_timer</code> \u2014 scheduled execution via systemd timers</li> <li>kinds: <code>kind: backup_job</code> \u2014 backup operations with retention and scheduling</li> <li>kinds: <code>kind: http_check</code> \u2014 GET-only HTTP readiness probe with retry/backoff</li> <li>kinds: <code>kind: postgres_ensure</code> \u2014 ensure PostgreSQL users, databases, extensions, grants</li> <li>cli: <code>loft-cli rotate-secret</code> \u2014 secret rotation as a day-2 operation</li> </ul>"},{"location":"changelog/#051-2024-09-15","title":"[0.5.1] - 2024-09-15","text":""},{"location":"changelog/#bug-fixes_3","title":"Bug Fixes","text":"<ul> <li>policy: Fix HMAC token validation for approval tokens with non-ASCII step IDs</li> <li>stack: Fix topological sort stability with large resource graphs</li> </ul>"},{"location":"changelog/#050-2024-09-01","title":"[0.5.0] - 2024-09-01","text":""},{"location":"changelog/#features_8","title":"Features","text":"<ul> <li>reconcile: <code>loft-cli doctor</code> \u2014 detect drift between desired and actual state</li> <li>reconcile: <code>loft-cli reconcile</code> \u2014 re-apply only drifted resources</li> <li>policy: Policy engine: <code>policy.yaml</code> with <code>auto_apply</code>, <code>require_approval</code>, <code>deny</code> actions</li> <li>policy: HMAC-SHA256 approval tokens with configurable TTL</li> <li>kinds: <code>kind: stack</code> \u2014 group related resources into a deployable application boundary</li> <li>kinds: Stack-aware dependency-ordered execution (topological sort with circular dep detection)</li> <li>env: Overlay / env-file layering \u2014 repeatable <code>--env-file</code> with explicit precedence</li> <li>registry: Addon registry + discovery via <code>entry_points</code> (7 open registries)</li> <li>monorepo: Three-package monorepo split: <code>loft-cli-core</code>, <code>loft-cli</code>, <code>loft-cli-agent</code></li> </ul>"},{"location":"changelog/#030-2024-06-01","title":"[0.3.0] - 2024-06-01","text":""},{"location":"changelog/#features_9","title":"Features","text":"<ul> <li>agent: <code>loft-cli-agent</code> binary \u2014 server-side executor with local plan execution</li> <li>agent: Transport protocol abstraction (<code>AgentTransport</code>, <code>FabricTransport</code>)</li> <li>agent: Server-side state tracking (<code>runtime-state.json</code>)</li> <li>agent: Mutation locking via <code>fcntl.flock</code></li> <li>kinds: <code>kind: file_template</code> \u2014 render managed configuration files from Jinja2 templates</li> <li>kinds: <code>kind: compose_project</code> \u2014 manage Docker Compose projects</li> </ul>"},{"location":"changelog/#020-2024-04-01","title":"[0.2.0] - 2024-04-01","text":""},{"location":"changelog/#features_10","title":"Features","text":"<ul> <li>tests: Smoke test suite (45 parametrized tests across 15 example specs)</li> <li>ci: Linux ARM64 binary support</li> <li>ci: macOS Intel + Apple Silicon split</li> <li>ci: PyPI publish workflow</li> <li>ci: <code>ruff</code> + <code>black</code> CI enforcement</li> <li>schema: Pydantic <code>extra='forbid'</code> on all spec models</li> <li>kinds: Nginx service kind \u2014 native Nginx support under <code>kind: service</code></li> </ul>"},{"location":"changelog/#010-2024-02-01","title":"[0.1.0] - 2024-02-01","text":""},{"location":"changelog/#features_11","title":"Features","text":"<ul> <li>bootstrap: Bootstrap spec \u2014 harden fresh Debian/Ubuntu servers</li> <li>service: PostgreSQL, Docker, container service kinds</li> <li>inventory: Local SQLite inventory with full historization</li> <li>safety: SSH lockout prevention gate</li> <li>safety: Goss server verification after bootstrap</li> <li>cli: Four-step workflow: <code>validate</code>, <code>plan</code>, <code>docs</code>, <code>apply</code></li> <li>distribution: <code>pip install loft-cli</code> via PyPI</li> <li>distribution: Standalone binaries for Linux (amd64/arm64) and macOS</li> <li>distribution: Docker image (<code>ghcr.io/1ops-eu/loft-cli</code>)</li> </ul>"},{"location":"cli-reference/","title":"CLI Reference","text":"<p>All loft-cli commands accept <code>--help</code> for usage details.</p>"},{"location":"cli-reference/#global-options","title":"Global Options","text":"<p>All commands that load specs support:</p> Option Description <code>--env-file PATH</code> Load <code>KEY=VALUE</code> pairs from a <code>.env</code> file. Repeatable \u2014 later files override earlier ones. Shell environment variables always take precedence. <code>--passthrough</code> Leave unresolved <code>${VAR}</code> references unchanged instead of erroring. Useful for generating docs or plans from specs with variables you don't want to resolve yet."},{"location":"cli-reference/#core-workflow","title":"Core Workflow","text":""},{"location":"cli-reference/#loft-cli-validate","title":"<code>loft-cli validate</code>","text":"<p>Check a spec file for errors without connecting to any server.</p> <pre><code>loft-cli validate &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p>Runs the Parse \u2192 Validate phases. Reports errors (fatal) and warnings (informational) with exact field paths. Safe to run at any time.</p>"},{"location":"cli-reference/#loft-cli-plan","title":"<code>loft-cli plan</code>","text":"<p>Generate and display the execution plan.</p> <pre><code>loft-cli plan &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p>Runs through Parse \u2192 Validate \u2192 Normalize \u2192 Plan and prints the ordered list of steps. No connection to the server is made. What you see here is exactly what <code>apply</code> will execute.</p>"},{"location":"cli-reference/#loft-cli-docs","title":"<code>loft-cli docs</code>","text":"<p>Generate a human-readable Markdown ops guide from the plan.</p> <pre><code>loft-cli docs &lt;spec.yaml&gt; [-o OUTPUT_FILE] [--mode guide|commands] [--env-file PATH]...\n</code></pre> Option Description <code>-o FILE</code> Write output to a file (default: stdout) <code>--mode guide</code> Full narrative guide with context (default) <code>--mode commands</code> Commands-only cheatsheet <p>The generated Markdown is suitable for a team wiki or runbook.</p>"},{"location":"cli-reference/#loft-cli-diff","title":"<code>loft-cli diff</code>","text":"<p>Show what would change on the server compared to its current runtime state.</p> <pre><code>loft-cli diff &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p>Compares the desired plan against the agent's recorded runtime state. Steps are classified as: - Added \u2014 new resource not yet applied - Changed \u2014 resource exists but content has changed - Unchanged \u2014 resource matches desired state (will be skipped) - Always-run \u2014 step runs regardless (e.g. health checks)</p> <p>Requires the agent to be installed and reachable on the target host.</p>"},{"location":"cli-reference/#loft-cli-apply","title":"<code>loft-cli apply</code>","text":"<p>Execute the plan on the target server.</p> <pre><code>loft-cli apply &lt;spec.yaml&gt; [--dry-run] [--mode auto|agent|client] [--approval-token TOKEN] [--env-file PATH]...\n</code></pre> Option Description <code>--dry-run</code> Print the plan without executing <code>--mode auto</code> Auto-detect agent vs. legacy client mode (default) <code>--mode agent</code> Force agent mode <code>--mode client</code> Force legacy Fabric client mode (deprecated) <code>--approval-token TOKEN</code> Provide a policy approval token for <code>require_approval</code> steps <p>Apply is idempotent \u2014 re-running skips resources that already match the desired state.</p>"},{"location":"cli-reference/#drift-management","title":"Drift Management","text":""},{"location":"cli-reference/#loft-cli-doctor","title":"<code>loft-cli doctor</code>","text":"<p>Detect drift between the desired spec and the server's actual state.</p> <pre><code>loft-cli doctor &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p>Reports per-resource drift status without making any changes. Use this to audit servers between planned applies.</p>"},{"location":"cli-reference/#loft-cli-reconcile","title":"<code>loft-cli reconcile</code>","text":"<p>Re-apply only the resources that have drifted from the desired spec.</p> <pre><code>loft-cli reconcile &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p>Runs <code>doctor</code> internally and then applies only the delta. Resources that match their desired state are skipped.</p>"},{"location":"cli-reference/#inventory","title":"Inventory","text":""},{"location":"cli-reference/#loft-cli-inventory-list","title":"<code>loft-cli inventory list</code>","text":"<p>List all servers in the local inventory.</p> <pre><code>loft-cli inventory list\n</code></pre>"},{"location":"cli-reference/#loft-cli-inventory-show","title":"<code>loft-cli inventory show</code>","text":"<p>Show full details for a specific server.</p> <pre><code>loft-cli inventory show &lt;server-id&gt;\n</code></pre>"},{"location":"cli-reference/#wireguard-tunnel","title":"WireGuard Tunnel","text":""},{"location":"cli-reference/#loft-cli-tunnel-up","title":"<code>loft-cli tunnel up</code>","text":"<p>Bring up the WireGuard tunnel for a host.</p> <pre><code>loft-cli tunnel up &lt;host&gt;\n</code></pre> <p>Creates a <code>wg-&lt;host&gt;</code> interface locally using the key material stored under <code>~/.wg/loft-cli/&lt;host&gt;/</code>. Requires <code>wireguard-tools</code> and passwordless <code>sudo</code> for <code>wg-quick</code>.</p>"},{"location":"cli-reference/#loft-cli-tunnel-down","title":"<code>loft-cli tunnel down</code>","text":"<p>Tear down the WireGuard tunnel for a host.</p> <pre><code>loft-cli tunnel down &lt;host&gt;\n</code></pre> <p>Removes the <code>wg-&lt;host&gt;</code> interface. The server's WireGuard interface remains active.</p>"},{"location":"cli-reference/#loft-cli-tunnel-status","title":"<code>loft-cli tunnel status</code>","text":"<p>List all managed hosts with their WireGuard state.</p> <pre><code>loft-cli tunnel status\n</code></pre> <p>Shows which hosts have WireGuard configured and whether the local interface is currently active.</p>"},{"location":"cli-reference/#maintenance","title":"Maintenance","text":""},{"location":"cli-reference/#loft-cli-version","title":"<code>loft-cli version</code>","text":"<p>Print the client version, and optionally the agent version on a remote host.</p> <pre><code>loft-cli version [--host HOST]\n</code></pre>"},{"location":"cli-reference/#loft-cli-update","title":"<code>loft-cli update</code>","text":"<p>Self-update the client binary from GitHub Releases.</p> <pre><code>loft-cli update\n</code></pre> <p>Downloads and replaces the current binary. Requires the binary to be installed (not pip-installed).</p>"},{"location":"cli-reference/#loft-cli-agent-update","title":"<code>loft-cli agent-update</code>","text":"<p>Update the <code>loft-cli-agent</code> binary on a remote host.</p> <pre><code>loft-cli agent-update &lt;host&gt;\n</code></pre> <p>Downloads the latest agent binary from GitHub Releases and replaces the installed agent on the server.</p>"},{"location":"cli-reference/#loft-cli-rotate-secret","title":"<code>loft-cli rotate-secret</code>","text":"<p>Rotate a named secret and re-apply the spec.</p> <pre><code>loft-cli rotate-secret &lt;spec.yaml&gt; --secret NAME [--env-file PATH]...\n</code></pre> <p>Generates a new value for the named secret, updates the server, and records the rotation in the inventory.</p>"},{"location":"cli-reference/#loft-cli-remove","title":"<code>loft-cli remove</code>","text":"<p>Remove all local state for a host (SSH config entry, WireGuard keys, inventory record).</p> <pre><code>loft-cli remove &lt;host&gt; [--force]\n</code></pre> <p>Warning</p> <p>This only removes local state. It does not decommission the server itself.</p>"},{"location":"cli-reference/#loft-cli-inspect-run","title":"<code>loft-cli inspect run</code>","text":"<p>Inspect the details of a past run.</p> <pre><code>loft-cli inspect run &lt;run-id&gt;\n</code></pre> <p>Reads from <code>~/.loft-cli/runs/&lt;run-id&gt;.json</code> and displays per-step timing, status, and output.</p>"},{"location":"cli-reference/#wireguard-client-prerequisites","title":"WireGuard Client Prerequisites","text":"<p>The <code>tunnel</code> commands and the WireGuard safety gate during <code>apply</code> run <code>wg-quick</code> and <code>ip</code> via <code>sudo</code> on your local machine. This requires:</p> <ol> <li><code>wireguard-tools</code> installed locally</li> <li>Passwordless <code>sudo</code> for <code>wg</code>, <code>wg-quick</code>, and <code>ip</code></li> </ol> <pre><code># Grant passwordless sudo for WireGuard commands only:\n# Create /etc/sudoers.d/wireguard-loft-cli with:\n%sudo ALL=(ALL) NOPASSWD: /usr/bin/wg, /usr/bin/wg-quick, /usr/sbin/ip\n\n# Validate the syntax:\nsudo visudo -f /etc/sudoers.d/wireguard-loft-cli\n</code></pre> <p>If WireGuard is not enabled in your spec, these prerequisites do not apply.</p>"},{"location":"concepts/","title":"Concepts","text":"<p>This page explains the key ideas behind loft-cli: the Layer 1 model, the spec pipeline, the agent architecture, and the safety guarantees.</p>"},{"location":"concepts/#layer-1-infrastructure-compiler","title":"Layer 1: Infrastructure Compiler","text":"<p>loft-cli is Layer 1. It makes VMs ready and usable:</p> <ul> <li>Bootstrap and harden fresh servers</li> <li>Install services (PostgreSQL, Nginx, Docker)</li> <li>Deploy containers and Compose stacks</li> <li>Manage configuration files</li> <li>Detect drift and reconcile state</li> <li>Verify the server matches the spec</li> </ul> <p>loft-cli does not do Layer 2. Application-level orchestration \u2014 configuring SaaS products, importing workflows, seeding databases with business data, wiring services together via API calls \u2014 belongs in a separate orchestration layer.</p> <p>The boundary: if the outcome depends only on OS/infrastructure state, it's loft-cli. If it depends on application runtime state, it's not.</p>"},{"location":"concepts/#the-pipeline","title":"The Pipeline","text":"<p>Every loft-cli operation flows through the same six-phase pipeline:</p> <pre><code>YAML Spec\n  \u2514\u2500 Parse       \u2190 registry lookup: kind \u2192 model class\n       \u2514\u2500 Validate   \u2190 kind-specific semantic checks\n            \u2514\u2500 Normalize  \u2190 resolve paths, keys, secrets\n                 \u2514\u2500 Plan       \u2190 generate ordered list of Steps\n                      \u251c\u2500 Docs       \u2190 render Markdown runbook\n                      \u251c\u2500 Diff       \u2190 compare plan vs. runtime state\n                      \u2514\u2500 Apply      \u2190 execute steps via agent\n</code></pre> <p>Plan is the single source of truth. Both <code>docs</code> and <code>apply</code> are generated from the same <code>Plan</code> object. What you review in <code>loft-cli plan</code> is exactly what <code>loft-cli apply</code> executes.</p>"},{"location":"concepts/#phase-1-parse","title":"Phase 1: Parse","text":"<p>Reads the YAML file, resolves <code>${VAR}</code> references via the environment + <code>.env</code> files, and hydrates a typed Pydantic model (e.g. <code>BootstrapSpec</code>). Unresolved variables in strict mode are fatal \u2014 you get the exact field path.</p>"},{"location":"concepts/#phase-2-validate","title":"Phase 2: Validate","text":"<p>Kind-specific semantic validation: SSH port in valid range, WireGuard config complete, containers require Docker enabled, password auth disable requires at least one pubkey. Returns errors (fatal) and warnings (informational).</p>"},{"location":"concepts/#phase-3-normalize","title":"Phase 3: Normalize","text":"<p>Resolves everything the planner needs: relative paths, SSH key file contents, WireGuard key derivation, database secrets from env, local filesystem paths. After normalization, plan generation is pure and deterministic \u2014 no further I/O.</p>"},{"location":"concepts/#phase-4-plan","title":"Phase 4: Plan","text":"<p>Generates an ordered list of <code>Step</code> objects. Each step carries:</p> Field Purpose <code>scope</code> <code>REMOTE</code> (SSH), <code>LOCAL</code> (this machine), or <code>VERIFY</code> <code>kind</code> Dispatch key \u2014 <code>ssh_command</code>, <code>gate</code>, <code>local_file_write</code>, etc. <code>command</code> Shell command string <code>depends_on</code> Step indices that must succeed first <code>gate</code> If <code>true</code>, failure aborts the entire plan <p>The planner embeds file contents directly into steps so the Plan is fully self-contained and reproducible.</p>"},{"location":"concepts/#phase-5-execute-via-agent","title":"Phase 5: Execute (via Agent)","text":"<p>Since v0.3, all execution goes through the <code>loft-cli-agent</code> binary on the target server:</p> <ol> <li>Client connects via SSH, uploads the agent binary + serialized plan</li> <li>Client invokes <code>loft-cli-agent apply</code> on the server</li> <li>Agent executes all steps locally (no SSH round-trips per command)</li> <li>Client retrieves the result</li> </ol> <p>This means SSH restarts during bootstrap are a non-event \u2014 the agent continues operating locally.</p>"},{"location":"concepts/#phase-6-record","title":"Phase 6: Record","text":"<p>After execution: inventory is updated (SQLite with full historization), a run log is written to <code>~/.loft-cli/runs/</code>, and the SSH session is closed.</p>"},{"location":"concepts/#the-registry-system","title":"The Registry System","text":"<p>loft-cli uses seven open registries to dispatch by spec kind. This means new kinds and step types can be added by external addons without modifying core source files.</p> Registry Maps <code>SPEC_REGISTRY</code> <code>kind</code> \u2192 Pydantic model class <code>PLANNER_REGISTRY</code> <code>kind</code> \u2192 plan-builder function <code>NORMALIZER_REGISTRY</code> <code>kind</code> \u2192 normalizer function <code>VALIDATOR_REGISTRY</code> <code>kind</code> \u2192 validator function <code>STEP_HANDLER_REGISTRY</code> <code>step.kind</code> \u2192 executor handler <code>HOOKS_REGISTRY</code> <code>kind</code> \u2192 lifecycle hooks <code>RESOLVER_REGISTRY</code> <code>prefix</code> \u2192 value resolver <p>External addons register via Python <code>entry_points</code>:</p> <pre><code># addon's pyproject.toml\n[project.entry-points.\"loft_cli.addons\"]\nmy_addon = \"my_addon:register\"\n</code></pre>"},{"location":"concepts/#ssh-lockout-prevention","title":"SSH Lockout Prevention","text":"<p>The most important safety property of the bootstrap plan: steps that would lock you out of your server never execute unless the admin login gate passes first.</p> <pre><code>Step 10: [GATE] verify_admin_login_on_new_port\nStep 11: disable_root_login         (depends_on: [10])\nStep 12: disable_password_auth      (depends_on: [10])\n</code></pre> <p>If the gate fails, steps 11 and 12 are skipped. You keep root access and the server remains reachable.</p> <p>There are three gates in a full bootstrap with WireGuard:</p> <ol> <li>Gate 1 \u2014 verify admin login with key auth before changing the SSH port</li> <li>Gate 2 \u2014 verify admin login on the new port before disabling root/password auth</li> <li>Gate 3 (WireGuard only) \u2014 verify SSH connectivity through the VPN tunnel before deleting the open SSH rule</li> </ol>"},{"location":"concepts/#idempotency-and-drift-detection","title":"Idempotency and Drift Detection","text":"<p>loft-cli is designed to be safe to re-run at any time:</p> <ul> <li>Hash-based change detection \u2014 the agent compares the hash of desired state against recorded runtime state. Unchanged resources are skipped.</li> <li><code>loft-cli doctor</code> \u2014 compares the desired spec against actual server state and reports drift without making changes.</li> <li><code>loft-cli reconcile</code> \u2014 re-applies only the resources that have drifted.</li> </ul>"},{"location":"concepts/#policy-engine","title":"Policy Engine","text":"<p>The policy engine controls which plan steps execute automatically, which require explicit approval, and which are denied. It is inert by default \u2014 no <code>policy.yaml</code> means no restrictions.</p> <pre><code># /etc/loft-cli/policy.yaml on the server\nversion: \"1\"\ndefault_action: auto_apply\n\nrules:\n  - name: deny-root-commands\n    match_id: \"run_as_root_*\"\n    action: deny\n\n  - name: approve-schema-migrations\n    match_tags: [schema_migration]\n    action: require_approval\n</code></pre> <p>Approval tokens are HMAC-SHA256 signed, time-limited, and scoped to a specific server. Generate one with <code>loft-cli rotate-secret --generate-approval-token</code>.</p>"},{"location":"concepts/#multi-document-specs","title":"Multi-Document Specs","text":"<p>A single YAML file can contain multiple specs separated by <code>---</code>. All documents are processed in order using the same env files:</p> <pre><code>kind: postgres_ensure\n# ... database declarations\n---\nkind: compose_project\n# ... Compose stack\n</code></pre> <p>This lets you describe a complete application deployment in a single file.</p>"},{"location":"concepts/#three-package-monorepo","title":"Three-Package Monorepo","text":"<p>The codebase is split into three independent packages:</p> Package Purpose <code>loft-cli-core</code> Shared models, spec schemas, registry infrastructure, policy engine <code>loft-cli</code> Client CLI, compiler pipeline, transports, local state <code>loft-cli-agent</code> Server-side executor, state management, mutation locking <p>Import boundaries are strict: the agent may not import from the client; the client may not import from the agent. Both import from core.</p> <p>The agent binary is minimal \u2014 it contains only <code>loft-cli-core</code> and <code>loft-cli-agent</code> code (no Fabric, no sqlcipher, no paramiko). This keeps the binary small and the attack surface on servers minimal.</p>"},{"location":"contributing/","title":"Contributing to loft-cli","text":"<p>Thank you for your interest in contributing to loft-cli. This document covers the local development setup, testing requirements, PR process, and commit conventions.</p>"},{"location":"contributing/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11+</li> <li>make</li> <li>git</li> <li>wireguard-tools (optional \u2014 only needed for tunnel-related tests against a live host)</li> </ul>"},{"location":"contributing/#local-development-setup","title":"Local Development Setup","text":"<pre><code># Clone the repo\ngit clone https://github.com/1ops-eu/loft-cli.git\ncd loft-cli\n\n# Create a virtualenv and install all packages in editable mode plus dev deps\nmake dev\n\n# Verify the install\nloft-cli --help\nloft-cli-agent --help\n</code></pre> <p><code>make dev</code> installs all three packages (<code>loft-cli-core</code>, <code>loft-cli</code>, <code>loft-cli-agent</code>) in editable mode (<code>-e</code>) so local changes take effect immediately without reinstalling.</p>"},{"location":"contributing/#running-tests","title":"Running Tests","text":"<pre><code># Unit and integration tests (no live host needed)\nmake test\n\n# Tests requiring local sqlcipher3 (inventory/SQLite)\nmake test-local\n\n# All tests (unit + integration + local)\nmake test-all\n</code></pre>"},{"location":"contributing/#smoke-tests-no-live-host-needed","title":"Smoke Tests (no live host needed)","text":"<p>Smoke tests validate and plan all 48 example specs:</p> <pre><code>make smoke\n</code></pre> <p>This runs <code>loft-cli validate</code>, <code>loft-cli plan</code>, and <code>loft-cli docs</code> on every YAML in <code>examples/</code>. They run in CI on every push.</p>"},{"location":"contributing/#goss-integration-tests-requires-a-live-ubuntu-server","title":"Goss Integration Tests (requires a live Ubuntu server)","text":"<pre><code>make test-goss HOST=&lt;ip&gt; PORT=&lt;port&gt; USER=&lt;user&gt;\n</code></pre> <p>These are optional and not run in CI by default.</p>"},{"location":"contributing/#code-quality","title":"Code Quality","text":"<pre><code># Lint (ruff + black --check)\nmake lint\n\n# Auto-format\nmake fmt\n</code></pre> <p>CI enforces <code>ruff</code> and <code>black</code> on every push and PR. Run <code>make fmt &amp;&amp; make lint</code> before pushing.</p>"},{"location":"contributing/#commit-convention","title":"Commit Convention","text":"<p>loft-cli uses Conventional Commits for automated changelog generation. Commit messages must follow this format:</p> <pre><code>&lt;type&gt;[optional scope]: &lt;description&gt;\n\n[optional body]\n\n[optional footer]\n</code></pre> <p>Types:</p> Type When to use <code>feat</code> A new feature or capability <code>fix</code> A bug fix <code>docs</code> Documentation changes only <code>chore</code> Maintenance, dependency updates, tooling <code>refactor</code> Code restructuring with no behavior change <code>test</code> Adding or updating tests <code>ci</code> CI/CD pipeline changes <p>Examples:</p> <pre><code>feat(bootstrap): add allow_ports field to firewall block\nfix(agent): prevent state file corruption on concurrent apply\ndocs: add WireGuard setup prerequisites to README\nchore: bump mkdocs-material to 9.6\n</code></pre> <p>Breaking changes: Append <code>!</code> to the type or add a <code>BREAKING CHANGE:</code> footer:</p> <pre><code>feat!: remove deprecated client-mode transport\n\nBREAKING CHANGE: The --mode client flag is removed. Use --mode agent.\n</code></pre>"},{"location":"contributing/#pr-process","title":"PR Process","text":"<ol> <li> <p>Branch from <code>main</code> using <code>feature/&lt;short-description&gt;</code> naming:    <pre><code>git checkout -b feature/add-allow-ports\n</code></pre></p> </li> <li> <p>One concern per PR \u2014 keep PRs focused. A bug fix should not include unrelated refactors.</p> </li> <li> <p>Before opening a PR: <pre><code>make lint &amp;&amp; make test &amp;&amp; make smoke\n</code></pre>    All three must pass. Fix any failures before pushing.</p> </li> <li> <p>For new spec kinds or fields \u2014 the CLAUDE.md rules apply:</p> </li> <li>Read the relevant package README before changing anything</li> <li>Update all affected READMEs</li> <li>Add or update an example in <code>examples/</code></li> <li> <p>Add tests</p> </li> <li> <p>PR title \u2014 use Conventional Commits format: <code>feat: add allow_ports to bootstrap spec</code></p> </li> </ol>"},{"location":"contributing/#adding-a-new-spec-kind","title":"Adding a New Spec Kind","text":"<p>New spec kinds follow the registry pattern. Here's the checklist:</p> <ol> <li>Define the Pydantic model in <code>packages/core/loft_cli_core/specs/</code></li> <li>Register it in <code>packages/client/loft_cli/_builtins.py</code> (or an addon)</li> <li>Write the normalizer \u2014 adds a normalizer function to the <code>NORMALIZER_REGISTRY</code></li> <li>Write the validator \u2014 adds a validator function to the <code>VALIDATOR_REGISTRY</code></li> <li>Write the planner \u2014 adds a planner function to the <code>PLANNER_REGISTRY</code>; generates <code>Step</code> objects</li> <li>Write the step handler(s) \u2014 registers handler functions in <code>STEP_HANDLER_REGISTRY</code></li> <li>Add KindHooks if needed \u2014 inventory recording, SSH port fallback, key generation</li> <li>Create an example in <code>examples/&lt;kind-name&gt;/</code> with a spec YAML and <code>README.md</code></li> <li>Write tests \u2014 spec schema validation, planner output, any edge cases</li> <li>Update READMEs \u2014 relevant package READMEs and <code>CLAUDE.md</code></li> </ol>"},{"location":"contributing/#spec-schema-stability-policy","title":"Spec Schema Stability Policy","text":"<p>From v1.0 onwards, loft-cli follows these rules for spec schema changes:</p>"},{"location":"contributing/#what-is-stable","title":"What is stable","text":"<ul> <li>All existing spec fields documented in the reference docs \u2014 adding a value that was valid before v1.0 must continue to work</li> <li>The <code>kind</code> field and top-level structure of every spec kind</li> <li>The CLI command names and their required arguments</li> <li>The local state paths (<code>~/.loft-cli/</code>, <code>~/.ssh/conf.d/loft-cli/</code>, <code>~/.wg/loft-cli/</code>)</li> <li>The agent's state file format (<code>runtime-state.json</code>, <code>desired-state.json</code>)</li> </ul>"},{"location":"contributing/#what-can-change-in-a-minor-release-1x0","title":"What can change in a minor release (1.x.0)","text":"<ul> <li>Adding new optional fields to existing spec kinds (with sensible defaults)</li> <li>Adding new spec kinds</li> <li>Adding new CLI commands or options</li> <li>Deprecating fields (with a <code>DeprecationWarning</code> and at least one minor release of overlap)</li> </ul>"},{"location":"contributing/#what-requires-a-major-release-200","title":"What requires a major release (2.0.0)","text":"<ul> <li>Removing or renaming existing spec fields</li> <li>Changing the meaning of an existing field</li> <li>Removing CLI commands or changing required arguments</li> <li>Breaking changes to the agent state file format</li> <li>Changing the local state directory structure</li> </ul>"},{"location":"contributing/#deprecation-process","title":"Deprecation process","text":"<ol> <li>Mark the field as deprecated in the schema (<code>deprecated=True</code> in Pydantic <code>Field</code>)</li> <li>Emit a <code>DeprecationWarning</code> when the field is used</li> <li>Document the migration path in CHANGELOG.md</li> <li>Remove in the next major release</li> </ol>"},{"location":"contributing/#agentclient-version-compatibility","title":"Agent/client version compatibility","text":"<p>The client and agent must have matching major versions. A v1.x client requires a v1.x agent. The client enforces this at connection time and exits with a clear error if the versions are incompatible.</p>"},{"location":"contributing/#release-process","title":"Release Process","text":"<p>Releases are triggered by Git tags. Only maintainers create releases.</p>"},{"location":"contributing/#step-1-verify-the-branch-is-ready","title":"Step 1 \u2014 Verify the branch is ready","text":"<pre><code># All tests and smoke tests must pass before tagging\nmake lint &amp;&amp; make test &amp;&amp; make smoke\n</code></pre> <p>Fix any failures before continuing. Do not tag a broken branch.</p>"},{"location":"contributing/#step-2-update-changelogmd","title":"Step 2 \u2014 Update CHANGELOG.md","text":"<pre><code># Generate from Conventional Commits history (requires git-cliff)\nmake changelog\n</code></pre> <p>Review the output. Edit manually if any entry is unclear or miscategorised. The changelog entry for the new version should be at the top.</p>"},{"location":"contributing/#step-3-bump-versions","title":"Step 3 \u2014 Bump versions","text":"<p>Bump the version string in all four locations \u2014 they must be identical:</p> <pre><code>packages/core/pyproject.toml       \u2190 loft-cli-core package version\npackages/client/pyproject.toml     \u2190 loft-cli package version\npackages/agent/pyproject.toml      \u2190 loft-cli-agent package version\npyproject.toml                     \u2190 root (dev tooling / monorepo marker)\n</code></pre> <p><code>packages/core/loft_cli_core/__init__.py</code> reads the version dynamically from the installed package metadata \u2014 no manual edit needed there.</p> <p>Also update any version references in documentation:</p> <pre><code>README.md                          \u2190 installation examples, badge, version table\ndocs/                              \u2190 any version-pinned code blocks or references\n</code></pre>"},{"location":"contributing/#step-4-update-roadmapmd-and-claudemd","title":"Step 4 \u2014 Update ROADMAP.md and CLAUDE.md","text":"<p>Mark completed items with <code>\u2705</code> in both files:</p> <ul> <li>In <code>ROADMAP.md</code>: tick off every item delivered in this release under the relevant version heading</li> <li>In <code>CLAUDE.md</code>: update the \"Current Capabilities\" table to reflect the new version and any newly completed features</li> </ul>"},{"location":"contributing/#step-5-commit","title":"Step 5 \u2014 Commit","text":"<pre><code>git add -A\ngit commit -m \"chore(release): bump version to X.Y.Z\"\n</code></pre>"},{"location":"contributing/#step-6-tag-and-push","title":"Step 6 \u2014 Tag and push","text":"<pre><code>git tag vX.Y.Z\ngit push origin main\ngit push origin vX.Y.Z\n</code></pre> <p>Pushing the tag triggers the release pipeline in GitHub Actions.</p>"},{"location":"contributing/#step-7-what-ci-does-automatically","title":"Step 7 \u2014 What CI does automatically","text":"<p>Once the tag is pushed, GitHub Actions:</p> <ul> <li>Builds client binaries for Linux (amd64, arm64) and macOS (arm64)</li> <li>Builds agent binaries for Linux (amd64, arm64)</li> <li>Generates <code>checksums.txt</code> (SHA-256 of all binaries)</li> <li>Creates a GitHub Release with all assets attached</li> <li>Publishes all three packages (<code>loft-cli-core</code>, <code>loft-cli</code>, <code>loft-cli-agent</code>) to PyPI</li> <li>Builds and pushes the Docker image to <code>ghcr.io/1ops-eu/loft-cli</code></li> <li>Publishes updated docs to GitHub Pages</li> </ul>"},{"location":"contributing/#step-8-post-release-verification","title":"Step 8 \u2014 Post-release verification","text":"<p>After CI completes (~10 min), verify:</p> <pre><code># PyPI publish\npip install --upgrade loft-cli &amp;&amp; loft-cli version\n\n# GitHub Release\ngh release view vX.Y.Z\n\n# Docker image\ndocker pull ghcr.io/1ops-eu/loft-cli:X.Y.Z &amp;&amp; \\\n  docker run --rm ghcr.io/1ops-eu/loft-cli:X.Y.Z version\n\n# Docs site \u2014 confirm new version appears at https://1ops-eu.github.io/loft-cli\n</code></pre>"},{"location":"contributing/#summary-checklist","title":"Summary checklist","text":"<pre><code>[ ] make lint &amp;&amp; make test &amp;&amp; make smoke \u2014 all pass\n[ ] CHANGELOG.md updated (make changelog + review)\n[ ] Version bumped in all 4 pyproject.toml files\n[ ] README.md / docs version references updated\n[ ] ROADMAP.md \u2014 completed items ticked off\n[ ] CLAUDE.md \u2014 capabilities table updated\n[ ] Commit: chore(release): bump version to X.Y.Z\n[ ] git tag vX.Y.Z &amp;&amp; git push origin main &amp;&amp; git push origin vX.Y.Z\n[ ] CI passes (binaries, PyPI, Docker, GitHub Pages)\n[ ] Post-release: pip install, gh release view, docker pull, docs site\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks you from installation through your first successful server bootstrap.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11+ or a pre-built binary (no Python needed with the binary)</li> <li>SSH access to a fresh Debian or Ubuntu server as <code>root</code></li> <li>An SSH key pair (<code>ssh-keygen -t ed25519</code> if you don't have one)</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":"pip (recommended)Standalone binary (Linux)Standalone binary (macOS)Docker <pre><code>pip install loft-cli\nloft-cli --help\n</code></pre> <p>Download the binary for your platform from the Releases page:</p> <pre><code># Linux x86-64\ncurl -L https://github.com/1ops-eu/loft-cli/releases/latest/download/loft-cli-linux-amd64 \\\n  -o /usr/local/bin/loft-cli\nchmod +x /usr/local/bin/loft-cli\n\n# Linux ARM64\ncurl -L https://github.com/1ops-eu/loft-cli/releases/latest/download/loft-cli-linux-arm64 \\\n  -o /usr/local/bin/loft-cli\nchmod +x /usr/local/bin/loft-cli\n</code></pre> <pre><code># Apple Silicon (M1/M2/M3)\ncurl -L https://github.com/1ops-eu/loft-cli/releases/latest/download/loft-cli-macos-arm64 \\\n  -o /usr/local/bin/loft-cli\nchmod +x /usr/local/bin/loft-cli\n</code></pre> <pre><code>docker run --rm \\\n  -v ~/.ssh:/root/.ssh:ro \\\n  -v $(pwd):/workspace:ro \\\n  ghcr.io/1ops-eu/loft-cli:latest --help\n</code></pre> <p>Verify the install:</p> <pre><code>loft-cli version\n</code></pre>"},{"location":"getting-started/#your-first-spec","title":"Your First Spec","text":"<p>Create a file called <code>my-server.yaml</code>:</p> <pre><code>kind: bootstrap\n\nmeta:\n  name: my-first-server\n  description: Harden a fresh Ubuntu server\n\nhost:\n  name: prod-1\n  address: 203.0.113.10      # Replace with your server's IP\n  os_family: debian\n\nlogin:\n  user: root\n  private_key: ~/.ssh/id_ed25519\n  port: 22\n\nadmin_user:\n  name: deploy\n  groups:\n    - sudo\n  pubkeys:\n    - ~/.ssh/id_ed25519.pub\n\nssh:\n  port: 2222\n  disable_root_login: true\n  disable_password_auth: true\n\nfirewall:\n  provider: ufw\n  ssh_only: true\n\nlocal:\n  ssh_config:\n    enabled: true\n    host_alias: prod-1\n  inventory:\n    enabled: true\n</code></pre>"},{"location":"getting-started/#step-1-validate","title":"Step 1: Validate","text":"<p>Check the spec for errors before doing anything:</p> <pre><code>loft-cli validate my-server.yaml\n</code></pre> <p>If validation passes, you'll see a green confirmation. Errors are reported with the exact field path.</p>"},{"location":"getting-started/#step-2-preview-the-plan","title":"Step 2: Preview the Plan","text":"<p>See exactly what loft-cli will do \u2014 before it does anything:</p> <pre><code>loft-cli plan my-server.yaml\n</code></pre> <p>This prints a numbered list of steps. Review them carefully. The bootstrap plan is ~25 steps including three safety gates that prevent SSH lockouts.</p>"},{"location":"getting-started/#step-3-generate-ops-docs-optional","title":"Step 3: Generate Ops Docs (optional)","text":"<p>Generate a Markdown runbook you can put in your wiki:</p> <pre><code>loft-cli docs my-server.yaml -o PROD_1_BOOTSTRAP.md\n</code></pre>"},{"location":"getting-started/#step-4-apply","title":"Step 4: Apply","text":"<p>Execute the plan:</p> <pre><code>loft-cli apply my-server.yaml\n</code></pre> <p>loft-cli will:</p> <ol> <li>Connect to your server as <code>root</code></li> <li>Install the <code>loft-cli-agent</code> on the server</li> <li>Upload the plan and invoke the agent</li> <li>The agent executes all steps locally (no SSH round-trips per command)</li> <li>Run Goss server verification at the end</li> <li>Write a local SSH config entry and inventory record</li> </ol> <p>After apply, SSH directly to the server:</p> <pre><code>ssh prod-1    # uses the ~/.ssh/conf.d/ entry loft-cli created\n</code></pre> <p>Bootstrap complete</p> <p>Your server is now hardened: SSH key-only on port 2222, root login disabled, UFW firewall active, and admin user <code>deploy</code> configured.</p>"},{"location":"getting-started/#using-environment-variables","title":"Using Environment Variables","text":"<p>For sensitive values (IP addresses, passwords, key paths), use <code>${VAR}</code> references in your spec and load them from a <code>.env</code> file:</p> <pre><code>host:\n  address: ${SERVER_IP}\n\nlogin:\n  private_key: ${SSH_KEY_PATH}\n</code></pre> <pre><code># .env\nSERVER_IP=203.0.113.10\nSSH_KEY_PATH=~/.ssh/id_ed25519\n</code></pre> <pre><code>loft-cli apply my-server.yaml --env-file .env\n</code></pre> <p><code>--env-file</code> is repeatable. Later files override earlier ones. Shell environment variables always take precedence.</p>"},{"location":"getting-started/#next-install-services","title":"Next: Install Services","text":"<p>Once the server is bootstrapped, install services with <code>kind: service</code>:</p> <pre><code># Install PostgreSQL\nloft-cli apply postgres.yaml --env-file .env\n\n# Deploy a Docker container\nloft-cli apply app.yaml --env-file .env\n</code></pre> <p>See the Spec Reference for all available spec kinds.</p>"},{"location":"getting-started/#recommended-project-layout","title":"Recommended Project Layout","text":"<pre><code>my-project/\n  servers/\n    prod-1/\n      bootstrap.yaml      \u2190 kind: bootstrap\n      services.yaml       \u2190 kind: service, stack, etc.\n      .env                \u2190 per-server variables\n    staging-1/\n      bootstrap.yaml\n      services.yaml\n      .env\n  shared/\n    .env                  \u2190 shared variables (org SSH key, org name, ...)\n  templates/\n    nginx-site.conf.j2    \u2190 Jinja2 templates for kind: file_template\n</code></pre> <p>Apply with layered env files:</p> <pre><code>loft-cli apply servers/prod-1/bootstrap.yaml \\\n  --env-file shared/.env \\\n  --env-file servers/prod-1/.env\n</code></pre>"},{"location":"operations/drift-reconcile/","title":"Drift Detection and Reconciliation","text":"<p>After initial apply, servers can drift from their declared spec \u2014 packages updated manually, config files changed, services stopped, containers removed. loft-cli provides tools to detect and correct this drift.</p>"},{"location":"operations/drift-reconcile/#how-drift-works","title":"How Drift Works","text":"<p>After every successful apply, the agent records the desired state to <code>/var/lib/loft-cli/desired/desired-state.json</code> on the server. This file contains a hash for each applied resource.</p> <p>Drift occurs when the actual state on the server no longer matches the recorded desired state \u2014 for example, when a config file is manually edited or a service is stopped.</p>"},{"location":"operations/drift-reconcile/#detecting-drift","title":"Detecting Drift","text":"<pre><code>loft-cli doctor &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p><code>doctor</code> compares the desired plan against the runtime state and reports per-resource drift status. It does not make any changes.</p> <p>Example output:</p> <pre><code>\u2713  postgres      up-to-date\n\u2717  nginx         drifted  (config file changed)\n\u2713  app-container up-to-date\n\u2717  backup-job    missing  (timer not found)\n</code></pre> <p>A drift report is also saved to the server at <code>/var/lib/loft-cli/doctor-result.json</code>.</p>"},{"location":"operations/drift-reconcile/#previewing-changes","title":"Previewing Changes","text":"<p>Before reconciling, use <code>diff</code> to see exactly what would change:</p> <pre><code>loft-cli diff &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p>This shows each step classified as: - Unchanged \u2014 matches desired state, will be skipped - Changed \u2014 content has changed, will be re-applied - Added \u2014 new resource not yet applied - Always-run \u2014 runs every time regardless (e.g. health checks)</p>"},{"location":"operations/drift-reconcile/#reconciling-drift","title":"Reconciling Drift","text":"<pre><code>loft-cli reconcile &lt;spec.yaml&gt; [--env-file PATH]...\n</code></pre> <p><code>reconcile</code> runs <code>doctor</code> internally and then re-applies only the resources that have drifted. Resources that match their desired state are skipped.</p> <p>This is equivalent to <code>loft-cli apply</code> but scoped to only drifted resources \u2014 it will not re-apply everything.</p>"},{"location":"operations/drift-reconcile/#recommended-workflow","title":"Recommended Workflow","text":"<pre><code># 1. Check what's drifted\nloft-cli doctor servers/prod-1/services.yaml --env-file servers/prod-1/.env\n\n# 2. Preview what reconcile would do\nloft-cli diff servers/prod-1/services.yaml --env-file servers/prod-1/.env\n\n# 3. Apply only the drift\nloft-cli reconcile servers/prod-1/services.yaml --env-file servers/prod-1/.env\n</code></pre>"},{"location":"operations/drift-reconcile/#safe-re-apply","title":"Safe Re-Apply","text":"<p><code>loft-cli apply</code> is always safe to re-run. Hash-based change detection ensures unchanged resources are skipped. If you're not sure whether to use <code>reconcile</code> or <code>apply</code>, use <code>apply</code> \u2014 the result is the same.</p> <pre><code># These are equivalent if nothing has changed:\nloft-cli apply servers/prod-1/services.yaml --env-file servers/prod-1/.env\nloft-cli reconcile servers/prod-1/services.yaml --env-file servers/prod-1/.env\n</code></pre> <p>The difference is performance: <code>reconcile</code> only applies the delta; <code>apply</code> processes all steps but skips unchanged ones.</p>"},{"location":"operations/fleet/","title":"Fleet Operations","text":"<p>loft-cli supports light multi-host operations \u2014 running commands across a set of servers selected by tags, roles, or other attributes from your local inventory.</p>"},{"location":"operations/fleet/#fleet-selectors","title":"Fleet Selectors","text":"<p>Select hosts from the local inventory using attribute selectors:</p> <pre><code>kind: fleet_apply\n\nmeta:\n  name: deploy-workers\n\nselector:\n  role: worker\n  env: production\n\nspec:\n  # Spec to apply to each selected host\n  kind: compose_project\n  # ...\n</code></pre> <p>Selectors support:</p> Operator Example Matches Equality <code>role: worker</code> Hosts with <code>role=worker</code> Multiple <code>role: worker, env: prod</code> Hosts matching all conditions"},{"location":"operations/fleet/#sequential-apply","title":"Sequential Apply","text":"<p>Fleet operations apply specs sequentially across selected hosts, one host at a time. This ensures:</p> <ul> <li>A failure on one host stops the fleet apply before affecting other hosts</li> <li>You can review the result of each host before the next begins</li> <li>No simultaneous mutations across the fleet</li> </ul>"},{"location":"operations/fleet/#fleet-doctor","title":"Fleet Doctor","text":"<p>Check drift across all selected hosts at once:</p> <pre><code>loft-cli doctor servers/fleet-services.yaml\n</code></pre> <p>With a fleet selector spec:</p> <pre><code>kind: fleet_doctor\n\nselector:\n  role: worker\n\nspec:\n  # Spec to check on each selected host\n  kind: compose_project\n  # ...\n</code></pre> <p>The output shows drift status per host, making it easy to identify which servers have drifted.</p>"},{"location":"operations/fleet/#aggregated-output","title":"Aggregated Output","text":"<p>Fleet commands aggregate results across all selected hosts:</p> <pre><code>prod-worker-1  \u2713  up-to-date (3/3 resources)\nprod-worker-2  \u2717  drifted (1/3 resources: compose_project)\nprod-worker-3  \u2713  up-to-date (3/3 resources)\n\nSummary: 1 host drifted, 2 hosts up-to-date\n</code></pre>"},{"location":"operations/fleet/#failure-handling","title":"Failure Handling","text":"<p>By default, fleet apply stops on the first host failure. This conservative default prevents a bad deploy from rolling across the entire fleet.</p> <p>Future versions will support configurable failure modes (e.g. continue on failure, max-failures threshold).</p>"},{"location":"operations/fleet/#recommended-approach","title":"Recommended Approach","text":"<p>For small fleets, manage hosts individually with per-host specs and env files:</p> <pre><code>for host in prod-1 prod-2 prod-3; do\n  loft-cli apply servers/$host/services.yaml \\\n    --env-file shared/.env \\\n    --env-file servers/$host/.env\ndone\n</code></pre> <p>Use fleet selectors when you have a homogeneous group of hosts running the same spec and want to manage them as a unit.</p>"},{"location":"operations/secret-rotation/","title":"Secret Rotation","text":"<p>loft-cli provides <code>rotate-secret</code> as a first-class day-2 operation for rotating secrets without manual server access.</p>"},{"location":"operations/secret-rotation/#rotating-a-secret","title":"Rotating a Secret","text":"<pre><code>loft-cli rotate-secret &lt;spec.yaml&gt; --secret NAME [--env-file PATH]...\n</code></pre> <p>This command:</p> <ol> <li>Generates a new value for the named secret (or reads the new value from an updated env variable)</li> <li>Updates the secret on the server</li> <li>Re-applies the affected resources (e.g. restarts the application with the new secret)</li> <li>Records the rotation in the local inventory</li> </ol>"},{"location":"operations/secret-rotation/#example","title":"Example","text":"<p>Rotating a database password:</p> <pre><code># Update the value in your .env file first:\n# APP_DB_PASSWORD=new-secure-password-here\n\nloft-cli rotate-secret services.yaml \\\n  --secret APP_DB_PASSWORD \\\n  --env-file servers/prod-1/.env\n</code></pre>"},{"location":"operations/secret-rotation/#policy-approval-tokens","title":"Policy Approval Tokens","text":"<p>If the server has a <code>policy.yaml</code> with <code>require_approval</code> rules, some steps may require an approval token before they execute.</p> <p>Generate a time-limited token:</p> <pre><code>loft-cli rotate-secret --generate-approval-token --step-id &lt;STEP_ID&gt;\n</code></pre> <p>Pass the token during apply:</p> <pre><code>loft-cli apply spec.yaml --approval-token &lt;TOKEN&gt;\n</code></pre> <p>Tokens are:</p> <ul> <li>HMAC-SHA256 signed \u2014 tamper-evident</li> <li>Time-limited \u2014 expire after a configurable TTL (default: 1 hour)</li> <li>Server-scoped \u2014 keyed to the specific server, cannot be reused across hosts</li> <li>Validated locally \u2014 no network round-trip required</li> </ul>"},{"location":"operations/secret-rotation/#policy-engine","title":"Policy Engine","text":"<p>The policy engine controls step execution on the agent. Configure it via <code>/etc/loft-cli/policy.yaml</code> on the server:</p> <pre><code>version: \"1\"\ndefault_action: auto_apply\n\nrules:\n  - name: require-approval-for-schema-changes\n    match_tags: [schema_migration]\n    action: require_approval\n\n  - name: deny-root-commands\n    match_id: \"run_as_root_*\"\n    action: deny\n\n  - name: auto-apply-health-checks\n    match_kind: http_check\n    action: auto_apply\n</code></pre> <p>Rules are evaluated in order. The first matching rule wins. If no rule matches, <code>default_action</code> applies.</p> <p>Policy is inert by default \u2014 no <code>policy.yaml</code> means all steps execute without restriction.</p>"},{"location":"operations/secret-rotation/#rule-matching","title":"Rule Matching","text":"<p>Each rule can match on any combination of (all specified conditions must hold):</p> Field Matches Example <code>match_kind</code> Step kind (exact) <code>\"ssh_command\"</code>, <code>\"http_check\"</code> <code>match_id</code> Step ID (glob pattern) <code>\"install_*\"</code>, <code>\"*_migration\"</code> <code>match_tags</code> Any of the listed tags <code>[destructive, schema_migration]</code>"},{"location":"operations/wireguard/","title":"WireGuard Tunnel","text":"<p>loft-cli has first-class support for WireGuard VPN tunnels. When enabled in a bootstrap spec, the server is configured to accept SSH only over the VPN \u2014 making the server unreachable from the public internet after bootstrap.</p>"},{"location":"operations/wireguard/#prerequisites","title":"Prerequisites","text":"<p>The <code>tunnel</code> commands and the WireGuard safety gate during <code>apply</code> run <code>wg-quick</code> and <code>ip</code> via <code>sudo</code> on your local machine. You need:</p> <ol> <li> <p><code>wireguard-tools</code> installed locally</p> <pre><code># Ubuntu/Debian\nsudo apt install wireguard-tools\n\n# macOS\nbrew install wireguard-tools\n</code></pre> </li> <li> <p>Passwordless <code>sudo</code> for the WireGuard commands</p> <pre><code># Create /etc/sudoers.d/wireguard-loft-cli:\n%sudo ALL=(ALL) NOPASSWD: /usr/bin/wg, /usr/bin/wg-quick, /usr/sbin/ip\n\n# Validate the syntax:\nsudo visudo -f /etc/sudoers.d/wireguard-loft-cli\n</code></pre> <p>Without passwordless sudo, the subprocess will hang waiting for a password prompt and time out after 30 seconds.</p> </li> </ol>"},{"location":"operations/wireguard/#enabling-wireguard-in-bootstrap","title":"Enabling WireGuard in Bootstrap","text":"<p>Add a <code>wireguard</code> block to your bootstrap spec:</p> <pre><code>wireguard:\n  enabled: true\n  interface: wg0\n  address: 10.10.0.1/24           # Server's VPN IP\n  private_key_file: .secrets/wg.key  # Path to the server's WireGuard private key\n  endpoint: vpn.example.com:51820 # Public endpoint of the WireGuard server\n  peer_address: 10.10.0.2/32      # Client's VPN IP\n  persistent_keepalive: 25\n</code></pre> <p>Generating a WireGuard private key:</p> <pre><code>mkdir -p .secrets\nwg genkey &gt; .secrets/wg.key\nchmod 600 .secrets/wg.key\n</code></pre>"},{"location":"operations/wireguard/#what-happens-during-bootstrap","title":"What Happens During Bootstrap","text":"<p>When <code>wireguard.enabled: true</code>, the bootstrap plan includes:</p> <ol> <li>Generates a client key pair (stored locally in <code>~/.wg/loft-cli/&lt;host&gt;/</code>)</li> <li>Installs WireGuard on the server</li> <li>Configures the server's WireGuard interface with the provided keys and peer</li> <li>Starts the WireGuard interface</li> <li>Adds a UFW rule allowing SSH only from the WireGuard peer address</li> <li>Tunnel safety gate \u2014 brings up the WireGuard tunnel locally, verifies SSH through the VPN IP</li> <li>If the gate passes: deletes the open SSH rule (SSH is now VPN-only)</li> <li>If the gate fails: tears down the tunnel, keeps the open SSH rule (server still accessible via public IP)</li> <li>Writes the SSH config entry using the VPN IP as <code>HostName</code></li> </ol>"},{"location":"operations/wireguard/#daily-use","title":"Daily Use","text":""},{"location":"operations/wireguard/#bring-up-the-tunnel","title":"Bring Up the Tunnel","text":"<pre><code>loft-cli tunnel up prod-1\n</code></pre> <p>Creates a <code>wg-prod-1</code> interface on your local machine using the key material from <code>~/.wg/loft-cli/prod-1/</code>.</p>"},{"location":"operations/wireguard/#ssh-through-the-tunnel","title":"SSH Through the Tunnel","text":"<pre><code>ssh prod-1\n</code></pre> <p>The SSH config entry uses the VPN IP (e.g. <code>10.10.0.2</code>) as <code>HostName</code>, so <code>ssh prod-1</code> automatically routes through the tunnel.</p>"},{"location":"operations/wireguard/#check-tunnel-status","title":"Check Tunnel Status","text":"<pre><code>loft-cli tunnel status\n</code></pre> <p>Lists all managed hosts and whether their WireGuard tunnel is currently active.</p>"},{"location":"operations/wireguard/#tear-down-the-tunnel","title":"Tear Down the Tunnel","text":"<pre><code>loft-cli tunnel down prod-1\n</code></pre> <p>Removes the <code>wg-prod-1</code> interface. The server's WireGuard interface remains active \u2014 SSH is still VPN-only.</p>"},{"location":"operations/wireguard/#per-host-interface-naming","title":"Per-Host Interface Naming","text":"<p>Each host gets its own local WireGuard interface: <code>wg-&lt;host&gt;</code>. This means you can have multiple tunnels active simultaneously without conflict:</p> <pre><code>loft-cli tunnel up prod-1      # creates wg-prod-1\nloft-cli tunnel up staging-1   # creates wg-staging-1\nloft-cli tunnel status         # shows both active\n</code></pre>"},{"location":"operations/wireguard/#key-material-storage","title":"Key Material Storage","text":"<p>WireGuard key material is stored locally at <code>~/.wg/loft-cli/&lt;host&gt;/</code>:</p> <pre><code>~/.wg/loft-cli/prod-1/\n  client.conf          \u2190 WireGuard client config (wg-quick format)\n  client_private.key   \u2190 Client private key\n  client_public.key    \u2190 Client public key\n  metadata.json        \u2190 Interface name, VPN IPs, endpoint\n</code></pre> <p>These files are generated once during bootstrap and reused on every <code>tunnel up</code>.</p>"},{"location":"operations/wireguard/#decommissioning","title":"Decommissioning","text":"<p>When removing a host, the WireGuard key material and tunnel state are cleaned up automatically:</p> <pre><code>loft-cli remove prod-1\n</code></pre> <p>This removes the SSH config entry, WireGuard keys, and inventory record. It does not touch the server itself.</p>"},{"location":"spec-reference/","title":"Spec Reference","text":"<p>loft-cli specs are YAML files with a <code>kind</code> field that determines what gets deployed. Every spec follows the same four-step pipeline: validate \u2192 plan \u2192 docs \u2192 apply.</p>"},{"location":"spec-reference/#all-spec-kinds","title":"All Spec Kinds","text":"Kind Description Platform <code>bootstrap</code> Harden a fresh Debian/Ubuntu server Debian, Ubuntu <code>service</code> Install PostgreSQL, Nginx, Docker, containers Any bootstrapped host <code>file_template</code> Render managed config files from Jinja2 templates Any bootstrapped host <code>compose_project</code> Deploy Docker Compose stacks Any host with Docker <code>stack</code> Group resources with dependency ordering Any bootstrapped host <code>http_check</code> HTTP readiness probe with retry/backoff Any bootstrapped host <code>systemd_unit</code> Host-native systemd services Debian, Ubuntu <code>systemd_timer</code> Scheduled execution via systemd timers Debian, Ubuntu <code>backup_job</code> Backup with retention and scheduling Debian, Ubuntu <code>postgres_ensure</code> Ensure PostgreSQL users, databases, extensions Any host with PostgreSQL"},{"location":"spec-reference/#common-fields","title":"Common Fields","text":"<p>Every spec has a <code>meta</code> block and a <code>host</code> block:</p> <pre><code>meta:\n  name: my-resource           # Required. Unique name for this resource.\n  description: My description # Optional. Human-readable description.\n\nhost:\n  name: prod-1                # Required. Matches the host_alias in your SSH config.\n  address: 203.0.113.10       # Required for bootstrap; optional if SSH config is set.\n  os_family: debian           # \"debian\" or \"rhel\"\n</code></pre> <p>And a <code>login</code> block (for specs that connect to a server):</p> <pre><code>login:\n  user: deploy                # SSH user\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n</code></pre>"},{"location":"spec-reference/#multi-document-specs","title":"Multi-Document Specs","text":"<p>A single YAML file can contain multiple specs separated by <code>---</code>. All documents are processed in order:</p> <pre><code>kind: postgres_ensure\nmeta:\n  name: app-db\n# ...\n---\nkind: compose_project\nmeta:\n  name: app-stack\n# ...\n</code></pre> <p>This is the recommended pattern for deploying a complete application: database resources first, then the application stack.</p>"},{"location":"spec-reference/#environment-variables","title":"Environment Variables","text":"<p>Use <code>${VAR}</code> references anywhere in a spec:</p> <pre><code>host:\n  address: ${SERVER_IP}\nlogin:\n  private_key: ${SSH_KEY_PATH}\n</code></pre> <p>Load values from a <code>.env</code> file:</p> <pre><code>loft-cli apply spec.yaml --env-file .env\n</code></pre> <p><code>--env-file</code> is repeatable. Shell environment variables always take precedence over <code>.env</code> files.</p>"},{"location":"spec-reference/backup-job/","title":"kind: backup_job","text":"<p>Defines backup operations with retention policy and scheduling via a systemd timer. Supports <code>postgres_dump</code> and <code>directory</code> backup types.</p>"},{"location":"spec-reference/backup-job/#what-it-does","title":"What It Does","text":"<ol> <li>Generates a backup script for the configured backup type</li> <li>Deploys it as a systemd oneshot service + timer (via <code>systemd_timer</code> primitives)</li> <li>Configures retention: prunes backups older than <code>retention_days</code></li> <li>Runs on the configured schedule</li> </ol>"},{"location":"spec-reference/backup-job/#example-postgresql-dump","title":"Example: PostgreSQL Dump","text":"<pre><code>kind: backup_job\n\nmeta:\n  name: db-backup\n  description: Nightly PostgreSQL backup with 30-day retention\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\nbackup:\n  type: postgres_dump\n  database: myapp\n  user: postgres\n  destination: /var/backups/myapp/postgres\n  retention_days: 30\n  schedule: \"03:00\"\n  compress: true\n</code></pre>"},{"location":"spec-reference/backup-job/#example-directory-backup","title":"Example: Directory Backup","text":"<pre><code>kind: backup_job\n\nmeta:\n  name: uploads-backup\n  description: Daily backup of uploaded files\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\nbackup:\n  type: directory\n  source: /opt/myapp/uploads\n  destination: /var/backups/myapp/uploads\n  retention_days: 14\n  schedule: \"04:00\"\n  compress: true\n</code></pre>"},{"location":"spec-reference/backup-job/#schema","title":"Schema","text":""},{"location":"spec-reference/backup-job/#backup","title":"<code>backup</code>","text":"Field Type Required Description <code>type</code> string Yes <code>postgres_dump</code> or <code>directory</code> <code>destination</code> string Yes Directory on the server where backups are stored <code>retention_days</code> int No Delete backups older than this many days (default: 30) <code>schedule</code> string No systemd calendar expression for when to run (default: <code>\"03:00\"</code>) <code>compress</code> bool No Compress backup files with gzip (default: true) <p>For <code>postgres_dump</code>:</p> Field Type Required Description <code>database</code> string Yes Database name to dump <code>user</code> string No PostgreSQL user (default: <code>postgres</code>) <code>host</code> string No PostgreSQL host (default: <code>127.0.0.1</code>) <code>port</code> int No PostgreSQL port (default: 5432) <p>For <code>directory</code>:</p> Field Type Required Description <code>source</code> string Yes Source directory to back up"},{"location":"spec-reference/backup-job/#backup-file-naming","title":"Backup File Naming","text":"<p>Backup files are named with a timestamp: <code>&lt;name&gt;-YYYY-MM-DD-HHmmss.sql.gz</code> (for postgres_dump) or <code>&lt;name&gt;-YYYY-MM-DD-HHmmss.tar.gz</code> (for directory).</p> <p>Retention cleanup runs after every backup, removing files in the destination directory older than <code>retention_days</code>.</p>"},{"location":"spec-reference/bootstrap/","title":"kind: bootstrap","text":"<p>Hardens a fresh Debian or Ubuntu server into a production-ready node with SSH key auth, a custom port, UFW firewall, and optional WireGuard VPN.</p> <p>This is the entry point for every loft-cli-managed server. It must be applied before any <code>service</code>, <code>compose_project</code>, or other spec kinds.</p>"},{"location":"spec-reference/bootstrap/#what-it-does","title":"What It Does","text":"<ol> <li>Detects the OS and installs base packages</li> <li>Creates an admin user with SSH key authentication</li> <li>Configures a custom SSH port</li> <li>Gate 1 \u2014 verifies admin login with key auth before proceeding</li> <li>Changes the SSH port in <code>sshd_config</code></li> <li>Opens the new port in UFW</li> <li>Gate 2 \u2014 verifies admin login on the new port before disabling root access</li> <li>Disables root login and password authentication</li> <li>Finalizes the firewall</li> <li>Sets up WireGuard (if enabled)</li> <li>Gate 3 (WireGuard) \u2014 verifies SSH through the tunnel before locking SSH to VPN-only</li> <li>Runs Goss server verification</li> <li>Writes local SSH config entry and inventory record</li> </ol> <p>Safety Gates</p> <p>The bootstrap plan contains three safety gates. Steps that would lock you out of the server never execute unless the preceding gate passes. If a gate fails, the plan aborts and you keep root access.</p>"},{"location":"spec-reference/bootstrap/#full-example","title":"Full Example","text":"<pre><code>kind: bootstrap\n\nmeta:\n  name: production-node-bootstrap\n  description: Harden a fresh Ubuntu node for production self-hosting\n\nhost:\n  name: prod-node-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: root\n  private_key: ~/.ssh/id_ed25519\n  port: 22\n\nadmin_user:\n  name: deploy\n  groups:\n    - sudo\n  pubkeys:\n    - ~/.ssh/id_ed25519.pub\n\nssh:\n  port: 2222\n  disable_root_login: true\n  disable_password_auth: true\n\nfirewall:\n  provider: ufw\n  ssh_only: true\n\nwireguard:\n  enabled: true\n  interface: wg0\n  address: 10.10.0.1/24\n  private_key_file: .secrets/wg.key\n  endpoint: vpn.example.com:51820\n  peer_address: 10.10.0.2/32\n  persistent_keepalive: 25\n\nlocal:\n  ssh_config:\n    enabled: true\n    host_alias: prod-node-1\n  inventory:\n    enabled: true\n    db_path: ~/.loft-cli/inventory.db\n</code></pre>"},{"location":"spec-reference/bootstrap/#schema","title":"Schema","text":""},{"location":"spec-reference/bootstrap/#meta","title":"<code>meta</code>","text":"Field Type Required Description <code>name</code> string Yes Unique name for this bootstrap resource <code>description</code> string No Human-readable description"},{"location":"spec-reference/bootstrap/#host","title":"<code>host</code>","text":"Field Type Required Description <code>name</code> string Yes Host alias used in SSH config and inventory <code>address</code> string Yes IP address or hostname of the target server <code>os_family</code> string Yes <code>debian</code> (covers Debian and Ubuntu)"},{"location":"spec-reference/bootstrap/#login","title":"<code>login</code>","text":"Field Type Required Description <code>user</code> string Yes Initial SSH user (typically <code>root</code>) <code>private_key</code> string Yes Path to the SSH private key <code>port</code> int No SSH port to connect on (default: 22)"},{"location":"spec-reference/bootstrap/#admin_user","title":"<code>admin_user</code>","text":"Field Type Required Description <code>name</code> string Yes Username for the new admin account <code>groups</code> list[string] No Additional groups (include <code>sudo</code> for sudo access) <code>pubkeys</code> list[string] Yes Paths to SSH public key files. At least one required when <code>disable_password_auth: true</code>."},{"location":"spec-reference/bootstrap/#ssh","title":"<code>ssh</code>","text":"Field Type Required Description <code>port</code> int No New SSH port (default: 22) <code>disable_root_login</code> bool No Disable root SSH login (default: false) <code>disable_password_auth</code> bool No Disable password authentication (default: false). Requires at least one pubkey."},{"location":"spec-reference/bootstrap/#firewall","title":"<code>firewall</code>","text":"Field Type Required Description <code>provider</code> string No Firewall provider \u2014 <code>ufw</code> (default) <code>ssh_only</code> bool No Enable firewall allowing only the configured SSH port (default: false) <code>allow_ports</code> list[int] No Additional ports to open in the firewall"},{"location":"spec-reference/bootstrap/#wireguard","title":"<code>wireguard</code>","text":"Field Type Required Description <code>enabled</code> bool No Enable WireGuard VPN configuration (default: false) <code>interface</code> string Yes (if enabled) WireGuard interface name on the server (e.g. <code>wg0</code>) <code>address</code> string Yes (if enabled) Server's WireGuard IP in CIDR notation (e.g. <code>10.10.0.1/24</code>) <code>private_key_file</code> string Yes (if enabled) Path to the server's WireGuard private key file <code>endpoint</code> string Yes (if enabled) Public endpoint for the WireGuard server (<code>host:port</code>) <code>peer_address</code> string Yes (if enabled) Client peer's IP in CIDR notation (e.g. <code>10.10.0.2/32</code>) <code>persistent_keepalive</code> int No Keepalive interval in seconds (default: 25)"},{"location":"spec-reference/bootstrap/#local","title":"<code>local</code>","text":"Field Type Required Description <code>ssh_config.enabled</code> bool No Write a <code>~/.ssh/conf.d/</code> entry (default: false) <code>ssh_config.host_alias</code> string No Alias for the SSH config entry (defaults to <code>host.name</code>) <code>ssh_config.config_path</code> string No Path to the SSH config file (default: <code>~/.ssh/config</code>) <code>inventory.enabled</code> bool No Record in local inventory (default: false) <code>inventory.db_path</code> string No Path to the inventory database (default: <code>~/.loft-cli/inventory.db</code>)"},{"location":"spec-reference/bootstrap/#after-bootstrap","title":"After Bootstrap","text":"<p>Once bootstrap completes, you can SSH to the server using the alias:</p> <pre><code>ssh prod-node-1\n</code></pre> <p>The SSH config entry uses the VPN IP as <code>HostName</code> when WireGuard is enabled. For WireGuard-enabled hosts, you need to bring up the tunnel first:</p> <pre><code>loft-cli tunnel up prod-node-1\nssh prod-node-1\n</code></pre> <p>See WireGuard Tunnel for the full tunnel workflow.</p>"},{"location":"spec-reference/compose-project/","title":"kind: compose_project","text":"<p>Manages a Docker Compose project on the server: uploads the compose file and configuration, validates, pulls images, starts services, and verifies health.</p> <p>Use <code>compose_project</code> to deploy multi-container applications described by a <code>docker-compose.yml</code> file.</p>"},{"location":"spec-reference/compose-project/#what-it-does","title":"What It Does","text":"<ol> <li>Uploads the <code>docker-compose.yml</code> to the target directory on the server</li> <li>Runs <code>docker compose config</code> to validate the compose file</li> <li>Pulls all images (<code>docker compose pull</code>)</li> <li>Starts services (<code>docker compose up -d</code>)</li> <li>Waits for all containers to reach healthy status (configurable timeout)</li> <li>Optionally runs <code>post_deploy</code> steps</li> </ol>"},{"location":"spec-reference/compose-project/#example","title":"Example","text":"<pre><code>kind: compose_project\n\nmeta:\n  name: app-stack\n  description: Main application Compose stack\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\nproject:\n  name: myapp\n  compose_file: docker-compose.yml\n  directory: /opt/myapp\n  env_file: .env.production\n  pull: true\n  health_check:\n    timeout_seconds: 120\n    interval_seconds: 5\n</code></pre>"},{"location":"spec-reference/compose-project/#schema","title":"Schema","text":""},{"location":"spec-reference/compose-project/#project","title":"<code>project</code>","text":"Field Type Required Description <code>name</code> string Yes Compose project name (sets <code>COMPOSE_PROJECT_NAME</code>) <code>compose_file</code> string Yes Path to the <code>docker-compose.yml</code>, relative to the spec file <code>directory</code> string Yes Destination directory on the server where the compose file is uploaded <code>env_file</code> string No Path to a <code>.env</code> file to upload alongside the compose file <code>pull</code> bool No Pull images before <code>up</code> (default: true) <code>health_check.timeout_seconds</code> int No Maximum time to wait for containers to become healthy (default: 60) <code>health_check.interval_seconds</code> int No How often to poll container health (default: 5)"},{"location":"spec-reference/compose-project/#health-checking","title":"Health Checking","text":"<p>After <code>docker compose up -d</code>, loft-cli polls the health status of all containers. A container is considered healthy when it either:</p> <ul> <li>Reports <code>healthy</code> status (if it defines a Docker healthcheck)</li> <li>Reaches <code>running</code> status and stays running (for containers without healthchecks)</li> </ul> <p>If any container fails to reach healthy status within <code>timeout_seconds</code>, the apply fails with a clear error showing which containers are unhealthy.</p>"},{"location":"spec-reference/compose-project/#idempotency","title":"Idempotency","text":"<p>On re-apply, loft-cli computes the hash of the compose file content and compares it to the recorded hash. If the compose file is unchanged and all containers are running, the project is considered up-to-date and the step is skipped.</p> <p>Changing the compose file (e.g. updating an image tag) causes the file to be re-uploaded and <code>docker compose up -d</code> to be re-run, which recreates only the affected services.</p>"},{"location":"spec-reference/compose-project/#combining-with-other-kinds","title":"Combining With Other Kinds","text":"<p><code>compose_project</code> is typically used within a <code>kind: stack</code> alongside <code>file_template</code> (for <code>.env</code> files) and <code>http_check</code> (for application-level readiness):</p> <pre><code>kind: stack\n# ...\nresources:\n  - kind: file_template     # Render .env file\n    # ...\n  - kind: compose_project   # Deploy the stack\n    depends_on: [env-file]\n    # ...\n  - kind: http_check        # Verify the app is serving\n    depends_on: [app-stack]\n    # ...\n</code></pre>"},{"location":"spec-reference/file-template/","title":"kind: file_template","text":"<p>Renders managed configuration files on the server from Jinja2 templates and variables. Change detection is hash-based \u2014 unchanged files are not re-written on re-apply.</p> <p>Use <code>file_template</code> when you need to deploy configuration files with dynamic values: Nginx configs with server names, application <code>.env</code> files, cron configs, etc.</p>"},{"location":"spec-reference/file-template/#what-it-does","title":"What It Does","text":"<ol> <li>Renders the Jinja2 template with the provided variables</li> <li>Computes a SHA-256 hash of the rendered content</li> <li>Compares the hash against the recorded runtime state</li> <li>If changed (or new), uploads the rendered file to the target path on the server</li> <li>Optionally runs a <code>reload_command</code> after the file is written</li> </ol>"},{"location":"spec-reference/file-template/#example","title":"Example","text":"<pre><code>kind: file_template\n\nmeta:\n  name: nginx-app-config\n  description: Nginx reverse proxy config for the app\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\ntemplate:\n  src: templates/nginx-app.conf.j2\n  dest: /etc/nginx/sites-available/app\n  owner: root\n  group: root\n  mode: \"0644\"\n  reload_command: systemctl reload nginx\n  vars:\n    server_name: app.example.com\n    upstream_port: 8080\n    worker_processes: 4\n</code></pre> <p>With the template file at <code>templates/nginx-app.conf.j2</code>:</p> <pre><code>server {\n    listen 80;\n    server_name {{ server_name }};\n\n    location / {\n        proxy_pass http://127.0.0.1:{{ upstream_port }};\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n    }\n}\n</code></pre>"},{"location":"spec-reference/file-template/#schema","title":"Schema","text":""},{"location":"spec-reference/file-template/#template","title":"<code>template</code>","text":"Field Type Required Description <code>src</code> string Yes Path to the Jinja2 template file, relative to the spec file <code>dest</code> string Yes Destination path on the server <code>owner</code> string No File owner on the server <code>group</code> string No File group on the server <code>mode</code> string No File permissions in octal string format (e.g. <code>\"0644\"</code>) <code>reload_command</code> string No Shell command to run on the server after the file is written <code>vars</code> dict No Variables to pass to the Jinja2 template"},{"location":"spec-reference/file-template/#template-rendering","title":"Template Rendering","text":"<p>Templates use standard Jinja2 syntax. Variables defined in <code>vars</code> are available directly by name:</p> <pre><code>{{ variable_name }}\n{% if condition %}...{% endif %}\n{% for item in items %}...{% endfor %}\n</code></pre> <p>Environment variables from <code>--env-file</code> are also available in the spec's <code>vars</code> values via <code>${VAR}</code> references:</p> <pre><code>vars:\n  db_password: ${DB_PASSWORD}\n  server_name: ${SERVER_NAME}\n</code></pre>"},{"location":"spec-reference/file-template/#idempotency","title":"Idempotency","text":"<p>On re-apply, loft-cli computes the SHA-256 hash of the rendered template (with current variable values) and compares it to the recorded hash. If unchanged, the upload is skipped and the <code>reload_command</code> is not run.</p> <p>This means changing a variable value (e.g. rotating a password) will cause the file to be re-rendered and re-uploaded on the next apply.</p>"},{"location":"spec-reference/http-check/","title":"kind: http_check","text":"<p>A GET-only HTTP readiness probe with retry/backoff. Returns success only when the endpoint responds with the expected status code within the configured retry budget.</p> <p>Use <code>http_check</code> as a post-deploy verification step, as a dependency gate inside a <code>kind: stack</code>, or as a standalone readiness check for any running service.</p>"},{"location":"spec-reference/http-check/#what-it-does","title":"What It Does","text":"<ol> <li>Makes a GET request to the configured URL</li> <li>Checks the response status code against <code>expected_status</code></li> <li>Retries with <code>retry_delay_seconds</code> intervals if the check fails</li> <li>Succeeds when the endpoint responds correctly within the retry budget</li> <li>Fails if all retries are exhausted</li> </ol>"},{"location":"spec-reference/http-check/#example","title":"Example","text":"<pre><code>kind: http_check\n\nmeta:\n  name: app-ready\n  description: Verify the application is serving traffic\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\ncheck:\n  url: http://localhost:3000/health\n  expected_status: 200\n  retries: 10\n  retry_delay_seconds: 5\n  timeout_seconds: 10\n</code></pre>"},{"location":"spec-reference/http-check/#schema","title":"Schema","text":""},{"location":"spec-reference/http-check/#check","title":"<code>check</code>","text":"Field Type Required Description <code>url</code> string Yes URL to check. The request is made from the server (not from your local machine). <code>expected_status</code> int No Expected HTTP response status code (default: 200) <code>retries</code> int No Number of retry attempts (default: 5) <code>retry_delay_seconds</code> int No Seconds to wait between retries (default: 5) <code>timeout_seconds</code> int No Per-request timeout in seconds (default: 10)"},{"location":"spec-reference/http-check/#notes","title":"Notes","text":"<p>The request is made from the server. Use <code>http://localhost:PORT</code> or internal IPs to check locally-running services. Do not use public domain names unless DNS resolution is available on the server at check time.</p> <p>GET only. <code>http_check</code> does not support POST, PUT, or other methods. For more complex checks, use a <code>kind: systemd_unit</code> with a custom health-check script.</p> <p>Suitable as a stack gate. Use <code>http_check</code> as the final resource in a <code>kind: stack</code> with <code>depends_on</code> pointing at the Compose project. The stack apply will not complete until the app is actually serving traffic.</p> <pre><code>resources:\n  - kind: compose_project\n    meta:\n      name: my-app\n    # ...\n\n  - kind: http_check\n    meta:\n      name: my-app-ready\n    depends_on:\n      - my-app\n    check:\n      url: http://localhost:8080/health\n      retries: 24\n      retry_delay_seconds: 5\n</code></pre>"},{"location":"spec-reference/postgres-ensure/","title":"kind: postgres_ensure","text":"<p>Ensures PostgreSQL resources exist on a running instance: users, databases, extensions, and grants. Structured declarations only \u2014 no arbitrary SQL.</p> <p>Use <code>postgres_ensure</code> for declarative database setup: create the application user, create the database, install extensions, and grant permissions \u2014 all idempotently.</p>"},{"location":"spec-reference/postgres-ensure/#what-it-does","title":"What It Does","text":"<ol> <li>Connects to the running PostgreSQL instance (via <code>psql</code> on the server)</li> <li>For each declared user: creates the role if it doesn't exist, sets the password</li> <li>For each declared database: creates it if it doesn't exist, sets the owner</li> <li>For each declared extension: creates it in the specified database if absent</li> <li>For each declared grant: applies the privilege grant</li> </ol> <p>All operations are idempotent \u2014 re-applying with the same declarations produces no changes.</p>"},{"location":"spec-reference/postgres-ensure/#example","title":"Example","text":"<pre><code>kind: postgres_ensure\n\nmeta:\n  name: app-db-setup\n  description: Create application database user, database, and extensions\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\nensure:\n  users:\n    - name: app\n      password_env: APP_DB_PASSWORD\n    - name: readonly\n      password_env: READONLY_DB_PASSWORD\n\n  databases:\n    - name: myapp\n      owner: app\n    - name: myapp_test\n      owner: app\n\n  extensions:\n    - database: myapp\n      name: pgcrypto\n    - database: myapp\n      name: uuid-ossp\n\n  grants:\n    - database: myapp\n      schema: public\n      privileges: [SELECT, INSERT, UPDATE, DELETE]\n      role: app\n    - database: myapp\n      schema: public\n      privileges: [SELECT]\n      role: readonly\n</code></pre>"},{"location":"spec-reference/postgres-ensure/#schema","title":"Schema","text":""},{"location":"spec-reference/postgres-ensure/#ensure","title":"<code>ensure</code>","text":""},{"location":"spec-reference/postgres-ensure/#ensureusers","title":"<code>ensure.users[]</code>","text":"Field Type Required Description <code>name</code> string Yes PostgreSQL role name <code>password_env</code> string Yes Environment variable containing the password"},{"location":"spec-reference/postgres-ensure/#ensuredatabases","title":"<code>ensure.databases[]</code>","text":"Field Type Required Description <code>name</code> string Yes Database name <code>owner</code> string No Owner role (must exist or be declared in <code>users</code>)"},{"location":"spec-reference/postgres-ensure/#ensureextensions","title":"<code>ensure.extensions[]</code>","text":"Field Type Required Description <code>database</code> string Yes Database to install the extension into <code>name</code> string Yes Extension name (e.g. <code>pgcrypto</code>, <code>uuid-ossp</code>, <code>vector</code>)"},{"location":"spec-reference/postgres-ensure/#ensuregrants","title":"<code>ensure.grants[]</code>","text":"Field Type Required Description <code>database</code> string Yes Database where the grant applies <code>schema</code> string No Schema to grant on (default: <code>public</code>) <code>privileges</code> list[string] Yes Privileges to grant (e.g. <code>[SELECT, INSERT]</code>) <code>role</code> string Yes Role to grant privileges to"},{"location":"spec-reference/postgres-ensure/#notes","title":"Notes","text":"<p>No arbitrary SQL. <code>postgres_ensure</code> only supports the structured operations listed above. For complex schema migrations or one-time SQL operations, use a <code>post_deploy</code> step in a <code>compose_project</code> or a <code>systemd_unit</code> with a migration script.</p> <p>Password handling. Passwords are passed via environment variables (never hardcoded in the spec). The environment variable is resolved on the client side and passed to the agent as part of the plan. The agent executes <code>ALTER ROLE ... PASSWORD '...'</code> on the server.</p> <p>Connection. By default, connects to <code>127.0.0.1:5432</code> as <code>postgres</code>. Customize via <code>ensure.postgres_host</code> and <code>ensure.postgres_port</code> if needed.</p>"},{"location":"spec-reference/service/","title":"kind: service","text":"<p>Installs services on an already-bootstrapped server: PostgreSQL, Nginx, Docker, and Docker containers.</p> <p><code>kind: service</code> is the multi-service spec kind \u2014 a single spec file can declare PostgreSQL, Nginx, Docker, and containers all together. Each block is optional; the planner only generates steps for populated blocks.</p>"},{"location":"spec-reference/service/#what-it-does","title":"What It Does","text":"<p>The <code>service</code> kind dispatches to sub-planners based on which blocks are present:</p> <ul> <li><code>postgres</code> \u2014 installs PostgreSQL, configures <code>listen_addresses</code>, optionally creates a role and database</li> <li><code>nginx</code> \u2014 installs Nginx, deploys site configuration, enables the site</li> <li><code>docker</code> \u2014 installs Docker Engine</li> <li><code>containers</code> \u2014 pulls images, creates and starts containers with health checks</li> </ul>"},{"location":"spec-reference/service/#example-postgresql","title":"Example: PostgreSQL","text":"<pre><code>kind: service\n\nmeta:\n  name: postgres-primary\n  description: Install PostgreSQL 16\n\nhost:\n  name: prod-node-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\npostgres:\n  enabled: true\n  version: \"16\"\n  listen_addresses:\n    - 127.0.0.1\n  create_role:\n    name: appuser\n    password_env: APP_DB_PASSWORD\n  create_database:\n    name: appdb\n    owner: appuser\n\nlocal:\n  inventory:\n    enabled: true\n</code></pre>"},{"location":"spec-reference/service/#example-docker-container","title":"Example: Docker + Container","text":"<pre><code>kind: service\n\nmeta:\n  name: app-services\n  description: Docker + application container\n\nhost:\n  name: prod-node-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\ndocker:\n  enabled: true\n\ncontainers:\n  - name: webapp\n    image: ghcr.io/acme/myapp:1.0.0\n    ports:\n      - \"8080:8080\"\n    env:\n      APP_ENV: production\n    restart: unless-stopped\n    healthcheck:\n      type: http\n      url: http://localhost:8080/health\n      expect_status: 200\n\nlocal:\n  inventory:\n    enabled: true\n</code></pre>"},{"location":"spec-reference/service/#schema","title":"Schema","text":""},{"location":"spec-reference/service/#postgres","title":"<code>postgres</code>","text":"Field Type Required Description <code>enabled</code> bool Yes Enable PostgreSQL installation <code>version</code> string No PostgreSQL version (e.g. <code>\"16\"</code>) <code>listen_addresses</code> list[string] No IP addresses PostgreSQL listens on <code>create_role.name</code> string No Role name to create <code>create_role.password_env</code> string No Environment variable containing the role password <code>create_database.name</code> string No Database name to create <code>create_database.owner</code> string No Owner role for the database"},{"location":"spec-reference/service/#nginx","title":"<code>nginx</code>","text":"Field Type Required Description <code>enabled</code> bool Yes Enable Nginx installation <code>sites</code> list No Site configurations to deploy <code>sites[].name</code> string Yes Site name (used as config filename) <code>sites[].server_name</code> string Yes <code>server_name</code> directive value <code>sites[].listen_port</code> int No Port to listen on (default: 80) <code>sites[].proxy_pass</code> string No Upstream URL for reverse proxy <code>sites[].root</code> string No Document root (for static sites)"},{"location":"spec-reference/service/#docker","title":"<code>docker</code>","text":"Field Type Required Description <code>enabled</code> bool Yes Enable Docker Engine installation"},{"location":"spec-reference/service/#containers","title":"<code>containers</code>","text":"<p>Each item in the <code>containers</code> list:</p> Field Type Required Description <code>name</code> string Yes Container name <code>image</code> string Yes Docker image (including tag) <code>ports</code> list[string] No Port mappings (<code>\"host:container\"</code>) <code>env</code> dict No Environment variables <code>volumes</code> list[string] No Volume mounts (<code>\"host:container\"</code>) <code>restart</code> string No Restart policy (<code>always</code>, <code>unless-stopped</code>, <code>on-failure</code>) <code>healthcheck.type</code> string No <code>http</code> or <code>tcp</code> <code>healthcheck.url</code> string No URL to poll (for <code>http</code> type) <code>healthcheck.expect_status</code> int No Expected HTTP status code <p>Note</p> <p>Containers require <code>docker.enabled: true</code> in the same spec (or Docker already installed on the server).</p>"},{"location":"spec-reference/stack/","title":"kind: stack","text":"<p>Groups related resources (<code>file_template</code>, <code>compose_project</code>, <code>http_check</code>, etc.) into a single deployable application boundary. Resources are declared inline with explicit dependency ordering and executed via topological sort.</p> <p>Use <code>stack</code> when you have multiple interdependent resources that should be deployed together: a config file, a Compose project, and a health check, for example.</p>"},{"location":"spec-reference/stack/#what-it-does","title":"What It Does","text":"<ol> <li>Validates dependency declarations for circular references (at validation time \u2014 no silent cycles)</li> <li>Sorts resources topologically by <code>depends_on</code></li> <li>Executes resources in order, each using its own planner</li> <li>Records all resources as <code>stack_resource</code> entries in the inventory</li> </ol>"},{"location":"spec-reference/stack/#example","title":"Example","text":"<pre><code>kind: stack\n\nmeta:\n  name: myapp\n  description: Full application stack \u2014 config, Compose, readiness check\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\nresources:\n  - kind: file_template\n    meta:\n      name: app-env\n    template:\n      src: templates/app.env.j2\n      dest: /opt/myapp/.env\n      vars:\n        db_host: 127.0.0.1\n        db_name: myapp\n        db_password: ${DB_PASSWORD}\n\n  - kind: compose_project\n    meta:\n      name: app-compose\n    depends_on:\n      - app-env\n    project:\n      name: myapp\n      compose_file: docker-compose.yml\n      directory: /opt/myapp\n\n  - kind: http_check\n    meta:\n      name: app-ready\n    depends_on:\n      - app-compose\n    check:\n      url: http://localhost:8080/health\n      expected_status: 200\n      retries: 20\n      retry_delay_seconds: 5\n</code></pre>"},{"location":"spec-reference/stack/#schema","title":"Schema","text":""},{"location":"spec-reference/stack/#top-level","title":"Top-level","text":"Field Type Required Description <code>meta.name</code> string Yes Stack name <code>host</code> object Yes Target host (same as other kinds) <code>login</code> object Yes SSH credentials <code>resources</code> list Yes List of resource declarations"},{"location":"spec-reference/stack/#resources","title":"<code>resources[]</code>","text":"<p>Each resource is a full spec-kind declaration with one addition:</p> Field Type Required Description <code>kind</code> string Yes Resource type: <code>file_template</code>, <code>compose_project</code>, <code>http_check</code>, <code>systemd_unit</code>, <code>systemd_timer</code>, <code>backup_job</code>, <code>postgres_ensure</code> <code>meta.name</code> string Yes Resource name \u2014 used as the <code>depends_on</code> reference key <code>depends_on</code> list[string] No Names of resources that must complete before this one starts (kind-specific fields) Same fields as the standalone spec kind"},{"location":"spec-reference/stack/#dependency-ordering","title":"Dependency Ordering","text":"<p>Resources are executed in the order determined by <code>depends_on</code> declarations. Circular dependencies are detected at validation time and cause a hard error \u2014 the plan is never generated.</p> <p>If multiple resources have no dependency relationship, they are executed in declaration order (stable sort).</p>"},{"location":"spec-reference/stack/#step-id-prefixing","title":"Step ID Prefixing","text":"<p>Steps generated from stack resources are prefixed with the resource name for traceability:</p> <pre><code>app-env/render_template\napp-env/upload_file\napp-compose/upload_compose_file\napp-compose/docker_compose_up\napp-ready/http_get\n</code></pre> <p>This makes run logs easy to navigate when a stack has many resources.</p>"},{"location":"spec-reference/systemd-timer/","title":"kind: systemd_timer","text":"<p>Deploys scheduled execution via systemd timers \u2014 a oneshot service unit paired with a <code>.timer</code> unit. The standard systemd alternative to cron.</p>"},{"location":"spec-reference/systemd-timer/#what-it-does","title":"What It Does","text":"<ol> <li>Writes a <code>&lt;name&gt;.service</code> unit file (type <code>oneshot</code>)</li> <li>Writes a <code>&lt;name&gt;.timer</code> unit file with the schedule</li> <li>Runs <code>systemctl daemon-reload</code></li> <li>Enables and starts the timer</li> </ol>"},{"location":"spec-reference/systemd-timer/#example","title":"Example","text":"<pre><code>kind: systemd_timer\n\nmeta:\n  name: nightly-cleanup\n  description: Nightly database cleanup job\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\ntimer:\n  name: nightly-cleanup\n  description: Run cleanup script every night at 2am\n  exec_start: /usr/local/bin/cleanup.sh --database myapp\n  user: deploy\n  on_calendar: \"02:00\"\n  persistent: true\n  environment:\n    DB_HOST: 127.0.0.1\n</code></pre>"},{"location":"spec-reference/systemd-timer/#schema","title":"Schema","text":""},{"location":"spec-reference/systemd-timer/#timer","title":"<code>timer</code>","text":"Field Type Required Description <code>name</code> string Yes Timer/service name <code>description</code> string No Description for both units <code>exec_start</code> string Yes Command to execute <code>user</code> string No User to run the command as <code>on_calendar</code> string Yes Systemd calendar expression (e.g. <code>\"02:00\"</code>, <code>\"Mon *-*-* 03:00:00\"</code>, <code>\"hourly\"</code>) <code>persistent</code> bool No Run the timer immediately if it was missed (default: true) <code>environment</code> dict No Environment variables for the service"},{"location":"spec-reference/systemd-timer/#calendar-expressions","title":"Calendar Expressions","text":"<p>systemd uses its own calendar specification format:</p> Schedule Expression Every day at 2am <code>02:00</code> Every hour <code>hourly</code> Every Monday at 3am <code>Mon 03:00</code> Every 15 minutes <code>*:0/15</code> Every day at midnight <code>daily</code> First day of the month <code>*-*-01 00:00:00</code> <p>See <code>man systemd.time</code> for the full specification.</p>"},{"location":"spec-reference/systemd-timer/#versus-kind-backup_job","title":"Versus <code>kind: backup_job</code>","text":"<p>For database backups, prefer <code>kind: backup_job</code> \u2014 it provides structured backup declarations with retention policy and is purpose-built for that use case. <code>systemd_timer</code> is for generic scheduled scripts.</p>"},{"location":"spec-reference/systemd-unit/","title":"kind: systemd_unit","text":"<p>Deploys and manages a host-native systemd service. Supports optional logrotate configuration for the service's log files.</p> <p>Use <code>systemd_unit</code> when you want to run a binary or script as a managed system service with automatic restart, resource limits, and proper init integration \u2014 without Docker.</p>"},{"location":"spec-reference/systemd-unit/#what-it-does","title":"What It Does","text":"<ol> <li>Writes the systemd unit file to <code>/etc/systemd/system/&lt;name&gt;.service</code></li> <li>Runs <code>systemctl daemon-reload</code></li> <li>Enables the service (<code>systemctl enable</code>)</li> <li>Starts the service (<code>systemctl start</code>)</li> <li>Optionally writes a logrotate config to <code>/etc/logrotate.d/&lt;name&gt;</code></li> </ol>"},{"location":"spec-reference/systemd-unit/#example","title":"Example","text":"<pre><code>kind: systemd_unit\n\nmeta:\n  name: my-worker\n  description: Background processing worker\n\nhost:\n  name: prod-1\n  address: 203.0.113.10\n  os_family: debian\n\nlogin:\n  user: deploy\n  private_key: ~/.ssh/id_ed25519\n  port: 2222\n\nunit:\n  name: my-worker\n  description: My background worker service\n  exec_start: /usr/local/bin/my-worker --config /etc/my-worker/config.toml\n  user: deploy\n  group: deploy\n  working_directory: /opt/my-worker\n  restart: always\n  restart_sec: 5\n  environment:\n    APP_ENV: production\n    LOG_LEVEL: info\n  log_file: /var/log/my-worker/worker.log\n  logrotate:\n    rotate: 7\n    compress: true\n    daily: true\n</code></pre>"},{"location":"spec-reference/systemd-unit/#schema","title":"Schema","text":""},{"location":"spec-reference/systemd-unit/#unit","title":"<code>unit</code>","text":"Field Type Required Description <code>name</code> string Yes Service name (used as the <code>.service</code> filename) <code>description</code> string No Systemd unit <code>Description</code> <code>exec_start</code> string Yes Command to execute (full path + arguments) <code>user</code> string No User to run the service as <code>group</code> string No Group to run the service as <code>working_directory</code> string No Working directory for the service <code>restart</code> string No Restart policy: <code>always</code>, <code>on-failure</code>, <code>unless-stopped</code> <code>restart_sec</code> int No Seconds to wait before restarting <code>environment</code> dict No Environment variables for the service <code>log_file</code> string No Path to the log file (used by logrotate)"},{"location":"spec-reference/systemd-unit/#unitlogrotate","title":"<code>unit.logrotate</code>","text":"Field Type Required Description <code>rotate</code> int No Number of rotations to keep (default: 7) <code>compress</code> bool No Compress rotated logs (default: true) <code>daily</code> bool No Rotate daily (default: true) <code>weekly</code> bool No Rotate weekly <code>postrotate</code> string No Shell command to run after rotation (e.g. reload the service)"},{"location":"spec-reference/systemd-unit/#idempotency","title":"Idempotency","text":"<p>On re-apply, loft-cli hashes the generated unit file content. If the unit file hasn't changed and the service is running, the step is skipped. If the unit file changes, the service is reloaded (or restarted) automatically.</p>"}]}